// SPDX-License-Identifier: BSL-1.0
// GENERATED by tools/amalgamate from include/tacit/_.hpp — DO NOT EDIT.
// Single-file form: copy this file anywhere and #include it; no other file is needed.
// single/_.hpp and single/$.hpp may be included together, in either order — the shared
// core is deduplicated by TACIT_SINGLE_CORE_SEEN.
#pragma once
#ifndef TACIT_SINGLE_CORE_SEEN
#define TACIT_SINGLE_CORE_SEEN

// Version as a comparable integer: MAJOR*10000 + MINOR*100 + PATCH. A clean-path macro (kept), so downstream code can
// `#if TACIT_VERSION >= 200` feature-test.
#define TACIT_VERSION_MAJOR 0
#define TACIT_VERSION_MINOR 4
#define TACIT_VERSION_PATCH 0
#define TACIT_VERSION (TACIT_VERSION_MAJOR * 10000 + TACIT_VERSION_MINOR * 100 + TACIT_VERSION_PATCH)
// ============================================================================================
//  _.hpp  —  the `_` object: closures from expressions, with a first-class
//            standard-library vocabulary
// ============================================================================================
//
//  `_` (a stateless global object of its own type `tacit::_`) whose members
//  return closures that forward to a same-named operation on whatever they are
//  later applied to:
//
//      _.size()        ==  [](auto&& x){ return std::ranges::size(x); }
//      _.push_back(y)  ==  [y](auto&& x){ return x.push_back(y); }
//      (_ == y)        ==  [y](auto&& x){ return x == y; }
//
//  BLANKS (partial application).  Each `_` token is one blank; the arity of the
//  closure is the number of blanks; blanks are filled left to right. The
//  receiver counts:
//
//      _.push_back(y)   -> 1 blank  (c)             c.push_back(y)
//      _.push_back(_)   -> 2 blanks (c, v)          c.push_back(v)
//      _.replace(_, _)  -> 3 blanks (c, a, b)       c.replace(a, b)
//      _ + _            -> 2 blanks (a, b)          a + b
//
//  Bound (non-`_`) arguments are stored by value; the receiver and blank-fills
//  are perfect- forwarded. Repeated `_` are DISTINCT blanks (there are no
//  positional `_1`/`_2` sigils — reach for a named lambda the moment you need
//  to reorder or reuse an argument).
//
//  COMPARISON CHAINS.  C++ parses `0 < _ < 10` as `(0 < _) < 10` — comparing a
//  *bool* against 10, so the closure would be silently always true. Comparison
//  sections instead chain, rewriting into the conjunction the notation means:
//
//      0 < _ < 10          -> x -> (0 < x) && (x < 10)
//      1 <= _.size() < 4   -> x -> (1 <= size(x)) && (size(x) < 4)
//
//  for any length and any mix of `== != < > <= >=`; every other operator ends
//  the chain. See "comparison chains" in namespace detail for the mechanism.
//
//  COMMA TUPLES.  `,` is the one section that builds data instead of calling
//  something, and the one that is n-ary — each further `,` appends an operand:
//
//      _, _          -> (a, b)    -> std::pair{a, b}
//      _, y          -> x         -> std::pair{x, y}
//      _, _, _       -> (a, b, c) -> std::tuple{a, b, c}
//      _.size(), _   -> (a, b)    -> std::pair{size(a), b}
//
//  Two operands are a `pair`, three or more a `tuple`; the list is flat, so
//  parenthesising concatenates rather than nests. Blanks stay distinct, so
//  `(_.size(), _.front())` takes TWO arguments — the same-input tuple is
//  `tacit::fanout(_.size(), _.front())`. A comma section composes onward through
//  the value it builds, keeping its arity: the vocabulary and the six comparisons
//  apply to the pair/tuple that comes out (`(_, _) == p`, `(_, _) < p`).
//
//  TEACH `_` YOUR OWN NAMES.  `_` is the one placeholder — there is no separate
//  derived object. Domain names are added to `_` in place, pre-#defined before
//  the include as bare comma lists: TACIT_VERBS (value-level member calls) and
//  TACIT_NOUNS (type-level nested-type projections):
//
//      #define TACIT_VERBS deposit, balance, freeze
//      #include <tacit/_.hpp>
//      using tacit::_;
//      // now first-class on the same `_`:
//      ranges::sort(accounts, {}, _.balance());
//      _.deposit(_)(acct, 100);            // blanks work, as everywhere
//
//  Each verb becomes a member on `_` (and on its projections and the arrow
//  proxy), guarded so a name a given type lacks is a clean SFINAE miss. Nouns are
//  the type-level twin, reached as `_::name::of<X>`.
//
//  THREE LAYERS
//    1. First-class vocabulary — generated from the curated tables (or your
//    own). Pure C++23
//       (builds on g++ 13 and clang 18, -std=c++23).
//    2. Operator sections — `_ == y`, `x + _`, `_ + _`. Finite and lexical.
//    3. Reflective hatch — `_.m<"custom">(a...)`, `_.field<"x">()`,
//    `_.enum_name()` for names not
//       in a table. C++26 (P2996); auto-detected, otherwise compiled out.
//
//  RANGE ACCESS goes through the customization points (`std::ranges::NAME(x)`,
//  not `x.NAME()`), so
//  `_.size()` / `_.begin()` also work on C arrays, string views, and
//  third-party ranges.
// ============================================================================================

#include <algorithm>
#include <array>
#include <cmath>
#include <cstddef>
#include <cstdlib>
#include <functional>
#include <ranges>
#include <string_view>
#include <tuple>
#include <type_traits>
#include <utility>
#include <version>

// Detect reflection (P2996). `__cpp_impl_reflection` is the language facility (the `^^` operator and `[: :]` splicers);
// `__cpp_lib_reflection` is the <meta> library. Define TACIT_HAS_REFLECTION yourself before including to force it (e.g.
// on the clang-p2996 fork behind -freflection-latest).
#ifndef TACIT_HAS_REFLECTION
#if defined(__cpp_impl_reflection) && defined(__cpp_lib_reflection)
#define TACIT_HAS_REFLECTION 1
#else
#define TACIT_HAS_REFLECTION 0
#endif
#endif
#if TACIT_HAS_REFLECTION
#include <meta>
#endif

namespace tacit {
namespace detail {

// TERMINOLOGY (one word, deliberately). `_` is the **placeholder** — the object, the thing you write. A **blank** is
// the gap it leaves: a position awaiting a fill, at term level (`_.push_back(_)`) or at type level (`tacit::blank<>`
// among a template's arguments). There is no third word: an earlier `hole` was a synonym for the type-level blank and
// is gone, since two names for one idea made `is_blank_v` and `hole<>` look like different mechanisms when they are the
// same one seen from the two levels. "Placeholder" is also the standard's own word (`std::placeholders`,
// `std::is_placeholder`) and the one this header's `is_tacit_placeholder` tag already used.
//
// A blank is any tacit placeholder in argument position — detected via the `is_tacit_placeholder` tag that TACIT_CORE
// injects, so blanks work for the default `_` and for any derived placeholder.
template <class T>
constexpr bool is_blank_v = [] {
  if constexpr (requires { std::remove_cvref_t<T>::is_tacit_placeholder; })
    return bool(std::remove_cvref_t<T>::is_tacit_placeholder);
  else
    return false;
}();

// The composable projection wrapper `fn` (defined in full below, after the vocabulary tables). The forward declarations
// let the section machinery treat an `fn` argument as a *projected* blank, and let the operator/section macros
// constrain against it.
//
// `fn`'s second parameter is its CHAIN STATE — see "comparison chains" below. Everything that is not a comparison
// section builds `fn{f}`, i.e. `Last == nochain`: an ordinary, unchained projection.
struct nochain {};
// A closure of more than one fill (`_ < _`, `_.size() < _`, `_ , _`'s operator siblings). It rides in the same slot as
// the chain state because the two never coexist: a two-input form has no rightmost-operand to fold against. The point
// is `is_slot_v` below — a two-input closure in argument position is a bound VALUE (`_.sort(_ < _)` passes a
// comparator), not a projected blank, which only a one-fill closure can be.
struct nary {};
template <class> constexpr bool is_fn_v = false;
#ifdef TACIT_SIGILS
// The synthetic-sigil markers (see "SYNTHETIC SIGILS" below, after `fn` is complete — `marked`
// derives from it). Declared here because the unary sections above build them.
struct amp_tag;
struct star_tag;
template <class Tag, class F, class G> struct marked;
template <class Tag, class G, class F> [[nodiscard]] constexpr auto mark(G inner, F f);
#endif
template <class, class = nochain> struct fn;
template <class F, class L> constexpr bool is_fn_v<fn<F, L>> = true;
#ifdef TACIT_SIGILS
// A marked closure IS an `fn` — it derives from one — so every `not_fn` constraint in the header
// must see it that way. Saying so is also what keeps the sigil overloads unambiguous: without it the
// ordinary `fn op value` sections claim a marked right operand as a plain bound value, and the two
// candidates tie.
template <class Tag, class F, class G> constexpr bool is_fn_v<marked<Tag, F, G>> = true;
#endif
template <class> constexpr bool is_nary_v = false;
template <class F> constexpr bool is_nary_v<fn<F, nary>> = true;
template <class T>
concept not_fn = !is_fn_v<std::remove_cvref_t<T>>;

// ---- comparison chains ------------------------------------------------------------------------ C++ parses `0 < _ <
// 10` as `(0 < _) < 10`, which compares a *bool* against 10 — always true. A comparison section therefore carries one
// extra piece of state, `last`: a projection recovering the value of the comparison's RIGHTMOST operand as a function
// of the eventual fill. The next comparison then rewrites itself into the conjunction that the notation means:
//
//     (… op0 m) op y   ==   (… op0 m) && (m op y)        with m == last(x)
//
// so `0 < _ < 10` is `x -> (0 < x) && (x < 10)`, `0 < _.size() <= n` is `x -> (0 < size(x)) && (size(x) <= n)`, and the
// rule iterates for any length or mix of `== != < > <= >=`. Only those six build chain state; every other operator
// yields a plain `fn`, so a chain ends the moment the expression stops being a comparison.
//
// The rightmost operand is one of three things, hence three `last` shapes:
template <class Y> struct always { // a bound value: independent of the fill  (`_ < y`)
  Y y;
  constexpr Y const &operator()(auto &&...) const noexcept { return y; }
};
template <class Y> always(Y) -> always<Y>;
struct same { // the blank itself: the fill, untouched  (`y < _`)
  constexpr decltype(auto) operator()(auto &&x) const noexcept { return static_cast<decltype(x)>(x); }
};
// (the third is a projection — `y < _.size()` stores the `fn` itself as `last`.)

// Chain state for a bound operand: none if it is a placeholder (`_ < _` is a two-input comparator, not a link),
// otherwise the constant projection.
template <class Y> [[nodiscard]] constexpr auto last_of(Y const &y) {
  if constexpr (is_blank_v<Y>)
    return nochain{};
  else
    return always<Y>{y};
}

// `x ->* pm`, the member-pointer projection: native `->*` when the operand provides it (a raw
// pointer, or a class that overloads it), otherwise deref-then-select — which is what lets
// `_ ->* pm` reach through smart pointers and iterators, where no built-in `->*` exists. This is the
// `.*` gap-filler: `.*` is not overloadable, so a section is the only way to spell it point-free.
// Data members only in practice — `(*x).*pmf` is valid solely as the head of a call, so member
// FUNCTIONS stay with the arrow proxy (`_->f(args)`).
inline constexpr auto memsel = []<class X, class Y>(X &&x, Y const &y) -> decltype(auto) {
  if constexpr (requires { std::forward<X>(x)->*y; })
    return std::forward<X>(x)->*y;
  else
    return (*std::forward<X>(x)).*y;
};

// A "slot" the section fills from a supplied argument: a plain blank (`_`, identity) or an `fn` — a *projected* blank,
// whose projection is applied to the fill before the call.
template <class T>
constexpr bool is_slot_v = is_blank_v<T> || (is_fn_v<std::remove_cvref_t<T>> && !is_nary_v<std::remove_cvref_t<T>>);

// Where the blanks are in an operand list, and how a supplied fill reaches each one. Shared by the two things built out
// of operand lists: `section` (a partially-applied member call) and `comma_section` (a comma tuple). Each operand is a
// plain blank, a projected blank (an `fn`, whose projection is applied to the fill), or a bound value; fills land in
// the slots left to right.
template <class... Operand> struct slot_map {
  static constexpr std::size_t arity = sizeof...(Operand);
  static constexpr std::array<bool, arity> slot_at{is_slot_v<Operand>...};
  static constexpr std::array<bool, arity> proj_at{is_fn_v<std::remove_cvref_t<Operand>>...};
  static constexpr std::size_t slots = [] {
    std::size_t n = 0;
    for (bool s : slot_at)
      n += s;
    return n;
  }();
  static constexpr std::size_t slots_before(std::size_t i) {
    std::size_t n = 0;
    for (std::size_t j = 0; j < i; ++j)
      n += slot_at[j];
    return n;
  }

  // Operand I as a one-element tuple, ready to be `tuple_cat`ed into the resolved argument list.
  template <std::size_t I, class Operands, class Fills>
  static constexpr decltype(auto) pick(Operands const &ops, Fills &&fills) {
    if constexpr (proj_at[I]) // projected blank: apply the stored fn to the fill (materialised)
      return std::make_tuple(std::get<I>(ops)(std::get<slots_before(I)>(std::forward<Fills>(fills))));
    else if constexpr (slot_at[I]) // plain blank: the fill, untouched
      return std::forward_as_tuple(std::get<slots_before(I)>(std::forward<Fills>(fills)));
    else // bound value
      return std::forward_as_tuple(std::get<I>(ops));
  }
};

// A partially-applied operation carrying its bound args and blank markers. `invoke` performs the call once every
// argument is materialised; `bound` holds each original argument (a value, or a placeholder standing for a blank).
// Applying it to (subject, fills...) splices the fills into the blank positions, left to right, and calls `invoke`.
template <class Invoke, class... Bound> struct section {
  using map = slot_map<Bound...>;
  Invoke invoke;
  std::tuple<Bound...> bound;

  static constexpr std::size_t slots = map::slots;

  template <class X, class... F>
    requires(sizeof...(F) == slots)
  constexpr decltype(auto) operator()(X &&x, F &&...f) const {
    auto fills = std::forward_as_tuple(std::forward<F>(f)...);
    return [&]<std::size_t... Is>(std::index_sequence<Is...>) -> decltype(auto) {
      return std::apply(
          [&](auto &&...args) -> decltype(auto) {
            return invoke(std::forward<X>(x), std::forward<decltype(args)>(args)...);
          },
          // `move(fills)` so `pick`'s std::get yields F&& rather than collapsing to an lvalue —
          // fills ARE perfect-forwarded, and passing the moved tuple to every pick is sound because
          // each fill feeds exactly ONE slot (slots_before(I) is distinct per slot).
          std::tuple_cat(map::template pick<Is>(bound, std::move(fills))...));
    }(std::make_index_sequence<map::arity>{});
  }
};

template <class Invoke, class... A> [[nodiscard]] constexpr auto make_section(Invoke invoke, A &&...a) {
  return section<Invoke, std::decay_t<A>...>{invoke, {std::forward<A>(a)...}};
}

// ---- comma sections ---------------------------------------------------------------------------- `_, _` and friends.
// A comma section is an operand list that *builds data* rather than calling something: applied to one fill per blank,
// it returns the operands as a `std::pair` (two of them) or a `std::tuple` (three or more) — `(_, _)` is `(a, b) -> {a,
// b}`, `(_, 9)` is `x -> {x, 9}`.
//
// Unlike every other section it is n-ary and it ACCUMULATES: because `,` is left-associative, `_, _, _` parses as `(_,
// _), _`, so each further `,` appends an operand to the list rather than pairing with the closure built so far. That is
// why this is its own type and not an `fn` — an `fn` is unary, and the plain lambda the two-blank form used to return
// could neither grow nor compose, which is what left `_, _, _` a shape that only errored when called.
template <class... Ops> struct comma_section;
template <class> constexpr bool is_comma_v = false;
template <class... Ops> constexpr bool is_comma_v<comma_section<Ops...>> = true;
template <class T>
concept not_comma = !is_comma_v<std::remove_cvref_t<T>>;

// The definition lands after the vocabulary tables (as `fn`'s does), since a comma section carries that vocabulary too;
// these declarations are what the section machinery and the `,` overloads on `_` and `fn` need before then.
template <class... A> [[nodiscard]] constexpr comma_section<std::decay_t<A>...> make_comma(A &&...a);

#if TACIT_HAS_REFLECTION
template <std::size_t N> struct fixed_string {
  char v[N]{};
  consteval fixed_string(char const (&s)[N]) { std::copy_n(s, N, v); }
  constexpr std::string_view view() const noexcept { return {v, N - 1}; }
};
template <std::size_t N> fixed_string(char const (&)[N]) -> fixed_string<N>;
#endif

// `get` dispatch, for `_.get<0>()` / `_.get<int>()`. Tuple-like access is the one std vocabulary name that is spelled
// with a TEMPLATE argument, so it cannot ride the ordinary member table — and it is the one place `<…>` is reachable at
// all, since a template argument list after a `.` binds to a member function template rather than demanding that the
// name before it be a template.
//
// Two dispatch routes, in this order:
//   1. the free `get<…>(x)`, via the two-step (`using std::get;` + unqualified call) — this is the real
//      route for `tuple`, `pair`, `array`, `variant`, `ranges::subrange` and any user type with an ADL
//      `get`, none of which have a member `get` at all;
//   2. the member `x.template get<…>()`, for types that spell it that way instead.
//
// Index and type forms are separate overloads because a template parameter list is single-kind: `<0>` is a value and
// `<int>` is a type. Function templates — including member function templates — are the one construct that can be
// overloaded on parameter kind, which is what lets both live under `get`.
template <std::size_t I, class X> [[nodiscard]] constexpr decltype(auto) get_(X &&x) {
  using std::get;
  if constexpr (requires { get<I>(static_cast<X &&>(x)); })
    return get<I>(static_cast<X &&>(x));
  else
    return static_cast<X &&>(x).template get<I>();
}
template <class T, class X> [[nodiscard]] constexpr decltype(auto) get_(X &&x) {
  using std::get;
  if constexpr (requires { get<T>(static_cast<X &&>(x)); })
    return get<T>(static_cast<X &&>(x));
  else
    return static_cast<X &&>(x).template get<T>();
}
template <std::size_t I, class X>
concept has_get_at = requires(X &&x) { tacit::detail::get_<I>(static_cast<X &&>(x)); };
template <class T, class X>
concept has_get_type = requires(X &&x) { tacit::detail::get_<T>(static_cast<X &&>(x)); };

// The other std names spelled with a TYPE argument: `_.any_cast<int>()`, `_.duration_cast<seconds>()`. Each is reached
// UNQUALIFIED, by ADL at instantiation — P0846 (C++20) makes `f<T>(x)` treat `f` as a template name when ordinary
// lookup finds nothing, which is what lets `any_cast<T>(a)` resolve for a `std::any` without this header including
// `<any>`. That matters: the include list here is eleven headers, and pulling in `<any>`, `<variant>`, `<memory>` and
// `<chrono>` to name four functions most callers never touch would tax every TU for nothing. A caller with a `std::any`
// has already included `<any>`; one without has nothing for the name to apply to, and the concept below rejects it
// cleanly.
//
// These live in `detail`, not in the class, precisely so ordinary lookup finds nothing: inside the class a member of
// the same name would be found first and the ADL step never happen.
//
// NOT here: chrono's `floor`/`ceil`/`round`. `<cmath>` is included (for the free-function table), so `floor` IS visible
// to ordinary lookup and `floor<seconds>(x)` stops meaning what it looks like — verified misbehaving. `duration_cast`
// covers the same ground under a name nothing else claims. `std::ranges::to` is C++23 but shipped late (libstdc++ 14,
// libc++ 17); g++-13 and the clang-18 CI image do not have it. Feature-tested rather than assumed, so `_.to<C>()`
// simply is not declared where the library cannot back it — the same posture as TACIT_HAS_REFLECTION.
#if defined(__cpp_lib_ranges_to_container) && __cpp_lib_ranges_to_container >= 202202L
#define TACIT_HAS_RANGES_TO 1
#else
#define TACIT_HAS_RANGES_TO 0
#endif

#define TACIT_ADL_TFN(NAME)                                                                                            \
  template <class T, class X> [[nodiscard]] constexpr decltype(auto) NAME##_(X &&x) {                                  \
    return NAME<T>(static_cast<X &&>(x));                                                                              \
  }                                                                                                                    \
  template <class T, class X>                                                                                          \
  concept has_##NAME = requires(X &&x) { NAME<T>(static_cast<X &&>(x)); };
#define TACIT_STD_TFREES(X)                                                                                            \
  X(any_cast) X(holds_alternative) X(duration_cast) X(static_pointer_cast) X(dynamic_pointer_cast) X(const_pointer_cast)
TACIT_STD_TFREES(TACIT_ADL_TFN)

} // namespace detail

// clang-format off
//  Member-call forwarder. Variadic, so one entry covers every overload/arity. No-blank calls take a
//  guarded fast path (unary closure, requires-guarded so a bad name is a clean SFINAE rejection); a
//  call containing `_` builds a `section` whose arity grows by one per blank. The guard uses fresh
//  requires-parameters (xx, aa...) — it must not name the captured pack (clang rejects that).
#define TACIT_MEMBER(NAME)                                                                                             \
  template <class... A>                                                                                                \
  [[nodiscard]] static constexpr auto NAME(A&&... a) {                                                                 \
    if constexpr ((tacit::detail::is_slot_v<A> || ...))                                                                \
      return tacit::detail::make_section(                                                                              \
          [](auto&& x, auto&&... g) -> decltype(auto) {                                                                \
            return std::forward<decltype(x)>(x).NAME(std::forward<decltype(g)>(g)...);                                 \
          },                                                                                                           \
          std::forward<A>(a)...);                                                                                      \
    else                                                                                                               \
      return tacit::detail::fn{[... a = std::forward<A>(a)]<class X>(X&& x) -> decltype(auto)                          \
               /* the guard mirrors the BODY's call — captured args are const lvalues by then,                        \
                  so testing the original value categories would pass calls the body cannot make */                    \
               requires requires(X&& xx, std::decay_t<A> const&... aa) {                                               \
                 std::forward<X>(xx).NAME(aa...);                                                                      \
               } { return std::forward<X>(x).NAME(a...); }};                                                           \
  }                                                                                                                    \
  static_assert(true)

//  Arrow-member forwarder: the twin of TACIT_MEMBER but through `->`, so `_->NAME(args)` builds
//  x -> x->NAME(args) (the pointee's member, via the real operator->). Lives on the `arrow` proxy
//  that `_`'s operator-> hands back. No projected-blank path — a plain call forwarder.
#define TACIT_ARROW_MEMBER(NAME)                                                                                       \
  template <class... A>                                                                                                \
    requires(!(tacit::detail::is_blank_v<A> || ...))                                                                   \
  [[nodiscard]] static constexpr auto NAME(A&&... a) {                                                                 \
    return tacit::detail::fn{[... a = std::forward<A>(a)]<class X>(X&& x) -> decltype(auto)                            \
             requires requires(X&& xx, std::decay_t<A> const&... aa) { /* guard == body, as TACIT_MEMBER */            \
               std::forward<X>(xx)->NAME(aa...);                                                                       \
             } { return std::forward<X>(x)->NAME(a...); }};                                                            \
  }                                                                                                                    \
  static_assert(true)

//  Range-adaptor verb (opt-in, behind TACIT_VIEWS). Unlike TACIT_MEMBER it BINDS its arguments as
//  values — a predicate/transformer is a value, not a projected blank — and routes through
//  std::views::ADAPT, so `_.filter(pred)` is x -> views::filter(x, pred): a *unary* fn that keeps
//  chaining. `_.filter(p).take(2)` is then x -> views::take(views::filter(x, p), 2), a point-free lazy
//  pipeline, and `(nums)` applies it. Binding (not projecting) the callable is exactly what keeps the
//  result unary and chainable — a projected blank would make it a two-input section that can't chain.
#define TACIT_VIEW(NAME, ADAPT)                                                                                        \
  template <class... A> [[nodiscard]] static constexpr auto NAME(A&&... a) {                                           \
    return tacit::detail::fn{[... a = std::forward<A>(a)]<class X>(X&& x) -> decltype(auto)                            \
             requires requires(X&& xx, std::decay_t<A> const&... aa) { /* guard == body */                             \
               std::views::ADAPT(std::forward<X>(xx), aa...);                                                          \
             } { return std::views::ADAPT(std::forward<X>(x), a...); }};                                               \
  }

//  The range-adaptor table (opt-in). `transform` is deliberately absent — it already means the
//  optional/ranges monadic member in the value vocabulary, so it needs a precedence call before it can
//  double as a view. Add more here (join, common, keys, values, ...) as they earn their place.
#define TACIT_VIEW_VERBS(X)                                                                                            \
  X(filter, filter) X(take, take) X(drop, drop)                                                                        \
  X(take_while, take_while) X(drop_while, drop_while) X(reverse, reverse)

//  Apply a forwarder macro M to a comma-separated list of names, each terminated with `;`. This is
//  the engine behind the TACIT_VERBS / TACIT_NOUNS extension hooks: the same list is driven through a
//  different forwarder for `_`, its projections (`fn`), and the arrow proxy. Empty list -> nothing.
//  Handles up to 256 names via the classic recursive-rescan trick.
#define TACIT_PARENS ()
#define TACIT_EXPAND(...)   TACIT_EXPAND_C(TACIT_EXPAND_C(TACIT_EXPAND_C(TACIT_EXPAND_C(__VA_ARGS__))))
#define TACIT_EXPAND_C(...) TACIT_EXPAND_B(TACIT_EXPAND_B(TACIT_EXPAND_B(TACIT_EXPAND_B(__VA_ARGS__))))
#define TACIT_EXPAND_B(...) TACIT_EXPAND_A(TACIT_EXPAND_A(TACIT_EXPAND_A(TACIT_EXPAND_A(__VA_ARGS__))))
#define TACIT_EXPAND_A(...) __VA_ARGS__
#define TACIT_FE(M, a, ...) M(a); __VA_OPT__(TACIT_FE_AGAIN TACIT_PARENS (M, __VA_ARGS__))
#define TACIT_FE_AGAIN() TACIT_FE
#define TACIT_FOR_EACH(M, ...) __VA_OPT__(TACIT_EXPAND(TACIT_FE(M, __VA_ARGS__)))

//  Unary / binary customization-point forwarders (route through std::ranges niebloids).
#define TACIT_CPO1(NAME, CPO)                                                                                          \
  [[nodiscard]] static constexpr auto NAME() {                                                                         \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires requires(X&& xx) { CPO(xx); } { return CPO(x); }};                                               \
  }
#define TACIT_CPO2(NAME, CPO)                                                                                          \
  [[nodiscard]] static constexpr auto NAME() {                                                                         \
    return tacit::detail::fn{[]<class X, class Y>(X&& x, Y&& y) -> decltype(auto)                                      \
             requires requires(X&& xx, Y&& yy) { CPO(xx, yy); } {                                                      \
      return CPO(std::forward<X>(x), std::forward<Y>(y));                                                              \
    }, tacit::detail::nary{}};                                                                                         \
  }

//  One operator section (both one-sided forms and the two-blank form). Uses the enclosing `self`.
#define TACIT_SECTION(op)                                                                                              \
  template <class Y> requires tacit::detail::not_fn<Y> [[nodiscard]] friend constexpr auto operator op(self, Y&& y) {  \
    return tacit::detail::fn{[y = std::forward<Y>(y)](auto&& x) -> decltype(auto)                                      \
             { return std::forward<decltype(x)>(x) op y; }};                                                           \
  }                                                                                                                    \
  template <class X> requires tacit::detail::not_fn<X> [[nodiscard]] friend constexpr auto operator op(X&& x, self) {  \
    if constexpr (std::is_copy_constructible_v<std::remove_cvref_t<X>>)                                                \
      return tacit::detail::fn{[x = std::forward<X>(x)](auto&& y) -> decltype(auto)                                    \
               { return x op std::forward<decltype(y)>(y); }};                                                         \
    else { /* non-copyable LVALUE (e.g. a stream): bind by reference so `os << _` works. A                             \
              non-copyable RVALUE is REJECTED — binding a dying temporary by reference would hand                      \
              back a silently dangling closure. */                                                                     \
      static_assert(std::is_lvalue_reference_v<X>,                                                                     \
                    "binding a non-copyable temporary would dangle: name it first "                                    \
                    "(e.g. `std::ostringstream os; auto f = os << _;`)");                                              \
      return tacit::detail::fn{[&x](auto&& y) -> decltype(auto)                                                        \
               { return x op std::forward<decltype(y)>(y); }};                                                         \
    }                                                                                                                  \
  }                                                                                                                    \
  [[nodiscard]] friend constexpr auto operator op(self, self) {                                                        \
    return tacit::detail::fn{[](auto&& x, auto&& y) -> decltype(auto)                                                  \
             { return std::forward<decltype(x)>(x) op std::forward<decltype(y)>(y); },                                 \
             tacit::detail::nary{}};                                                                                   \
  }

//  One comparison section: TACIT_SECTION plus chain state, so a following comparison can rewrite
//  itself into the conjunction the notation means (`0 < _ < 10` == `(0 < x) && (x < 10)`). The
//  one-sided forms differ only in what the rightmost operand is: the bound value (`_ op y`) or the
//  blank (`x op _`). The two-blank form stays a two-INPUT comparator — nothing to chain onto.
#define TACIT_COMPARE(op)                                                                                              \
  template <class Y> requires tacit::detail::not_fn<Y> [[nodiscard]] friend constexpr auto operator op(self, Y&& y) {  \
    auto last = tacit::detail::last_of(y); /* before the capture below moves from y */                                 \
    return tacit::detail::fn{[y = std::forward<Y>(y)](auto&& x) -> decltype(auto)                                      \
             { return std::forward<decltype(x)>(x) op y; }, std::move(last)};                                          \
  }                                                                                                                    \
  template <class X> requires tacit::detail::not_fn<X> [[nodiscard]] friend constexpr auto operator op(X&& x, self) {  \
    if constexpr (std::is_copy_constructible_v<std::remove_cvref_t<X>>)                                                \
      return tacit::detail::fn{[x = std::forward<X>(x)](auto&& y) -> decltype(auto)                                    \
               { return x op std::forward<decltype(y)>(y); }, tacit::detail::same{}};                                  \
    else { /* non-copyable lvalue: by reference; non-copyable rvalue: rejected, as TACIT_SECTION */                    \
      static_assert(std::is_lvalue_reference_v<X>,                                                                     \
                    "binding a non-copyable temporary would dangle: name it first");                                   \
      return tacit::detail::fn{[&x](auto&& y) -> decltype(auto)                                                        \
               { return x op std::forward<decltype(y)>(y); }, tacit::detail::same{}};                                  \
    }                                                                                                                  \
  }                                                                                                                    \
  [[nodiscard]] friend constexpr auto operator op(self, self) {                                                        \
    return tacit::detail::fn{[](auto&& x, auto&& y) -> decltype(auto)                                                  \
             { return std::forward<decltype(x)>(x) op std::forward<decltype(y)>(y); },                                 \
             tacit::detail::nary{}};                                                                                   \
  }

//  The `->*` section — same three forms as TACIT_SECTION, but routed through `memsel` so the
//  operator keeps its NATURAL meaning (member-pointer projection) on anything dereferenceable:
//  `_ ->* &W::x` == `p -> (*p).x`, on raw pointers, smart pointers and iterators alike.
#define TACIT_MEMPTR                                                                                                   \
  template <class Y> requires tacit::detail::not_fn<Y> [[nodiscard]] friend constexpr auto operator->*(self, Y&& y) {  \
    return tacit::detail::fn{[y = std::forward<Y>(y)](auto&& x) -> decltype(auto)                                      \
             { return tacit::detail::memsel(std::forward<decltype(x)>(x), y); }};                                      \
  }                                                                                                                    \
  template <class X> requires tacit::detail::not_fn<X> [[nodiscard]] friend constexpr auto operator->*(X&& x, self) {  \
    return tacit::detail::fn{[x = std::forward<X>(x)](auto&& y) -> decltype(auto)                                      \
             { return tacit::detail::memsel(x, std::forward<decltype(y)>(y)); }};                                      \
  }                                                                                                                    \
  [[nodiscard]] friend constexpr auto operator->*(self, self) {                                                        \
    return tacit::detail::fn{[](auto&& x, auto&& y) -> decltype(auto)                                                  \
             { return tacit::detail::memsel(std::forward<decltype(x)>(x), std::forward<decltype(y)>(y)); },            \
             tacit::detail::nary{}};                                                                                   \
  }

//  One unary operator (prefix). Coexists with a same-token binary section (`*_` vs `_ * y`) — they
//  differ by arity. `&_` builds `x -> &x`, not the placeholder's address (use std::addressof if ever
//  needed); overloading unary `&` is the one to keep in mind.
//  How a unary section wraps its lambda. Plain by default; under TACIT_SIGILS the
//  `&` and `*` forms produce a TAGGED closure instead — one that still IS an `fn` (it derives), so
//  `(&_)(c) == &c` and `(*_) + 1` are unchanged, but which the synthetic sigils can dispatch on.
#define TACIT_PLAIN(L) tacit::detail::fn{L}
#define TACIT_MARK_INNER tacit_mark_inner_
#ifdef TACIT_SIGILS
#define TACIT_MARK_AMP(L) tacit::detail::mark<tacit::detail::amp_tag>(TACIT_MARK_INNER, L)
#define TACIT_MARK_STAR(L) tacit::detail::mark<tacit::detail::star_tag>(TACIT_MARK_INNER, L)
#else
#define TACIT_MARK_AMP(L) TACIT_PLAIN(L)
#define TACIT_MARK_STAR(L) TACIT_PLAIN(L)
#endif
#define TACIT_UNARY_AS(op, WRAP)                                                                                       \
  [[nodiscard]] friend constexpr auto operator op(self) {                                                              \
    [[maybe_unused]] auto tacit_mark_inner_ =                                                                           \
        tacit::detail::fn{[](auto&& x) -> decltype(auto) { return std::forward<decltype(x)>(x); }};                    \
    return WRAP([](auto&& x) -> decltype(auto)                                                                         \
             { return op std::forward<decltype(x)>(x); });                                                             \
  }
#define TACIT_UNARY(op) TACIT_UNARY_AS(op, TACIT_PLAIN)
//  Postfix form (the trailing int disambiguates), for ++ / --.
#define TACIT_UNARY_POST(op)                                                                                           \
  [[nodiscard]] friend constexpr auto operator op(self, int) {                                                         \
    return tacit::detail::fn{[](auto&& x) -> decltype(auto)                                                            \
             { return std::forward<decltype(x)>(x) op; }};                                                             \
  }
//  One compound-assignment section: `_ op y` == x -> (x op y). Mutating and left-operand-only (there
//  is no `y op _` form — that would assign into y). The argument binds by reference, so it mutates
//  the caller's lvalue: e.g. std::ranges::for_each(v, _ += 1).
#define TACIT_ASSIGN(op)                                                                                               \
  template <class Y> requires tacit::detail::not_fn<Y> [[nodiscard]] friend constexpr auto operator op(self, Y&& y) {  \
    if constexpr (tacit::detail::is_blank_v<Y>) /* `_ op= _` is two-input, like `_ op _` */                            \
      return tacit::detail::fn{[](auto&& a, auto&& b) -> decltype(auto)                                                \
               { return std::forward<decltype(a)>(a) op std::forward<decltype(b)>(b); },                               \
               tacit::detail::nary{}};                                                                                 \
    else                                                                                                               \
      return tacit::detail::fn{[y = std::forward<Y>(y)](auto&& x) -> decltype(auto)                                    \
               { return std::forward<decltype(x)>(x) op y; }};                                                         \
  }

//  Reflective members (defined empty when reflection is off, so TACIT_CORE need not branch).
#if TACIT_HAS_REFLECTION
#  define TACIT_REFLECT(Self)                                                                                          \
    template <tacit::detail::fixed_string Name, class... A>                                                            \
    [[nodiscard]] static constexpr auto m(A&&... a) {                                                                  \
      return [... a = std::forward<A>(a)]<class X>(X&& x) -> decltype(auto) {                                          \
        constexpr std::meta::info r = [] consteval {                                                                   \
          for (std::meta::info e : std::meta::members_of(                                                              \
                   ^^std::remove_cvref_t<X>, std::meta::access_context::current()))                                    \
            if (std::meta::is_function(e) && std::meta::has_identifier(e) &&                                           \
                std::meta::identifier_of(e) == Name.view())                                                            \
              return e;                                                                                                \
          return std::meta::info{};                                                                                    \
        }();                                                                                                           \
        return std::forward<X>(x).[:r:](a...);                                                                         \
      };                                                                                                               \
    }                                                                                                                  \
    template <tacit::detail::fixed_string Name>                                                                        \
    [[nodiscard]] static constexpr auto field() {                                                                      \
      return []<class X>(X&& x) -> decltype(auto) {                                                                    \
        constexpr std::meta::info r = [] consteval {                                                                   \
          for (std::meta::info e : std::meta::nonstatic_data_members_of(                                               \
                   ^^std::remove_cvref_t<X>, std::meta::access_context::current()))                                    \
            if (std::meta::identifier_of(e) == Name.view()) return e;                                                  \
          return std::meta::info{};                                                                                    \
        }();                                                                                                           \
        return std::forward<X>(x).[:r:];                                                                               \
      };                                                                                                               \
    }                                                                                                                  \
    [[nodiscard]] static constexpr auto enum_name() {                                                                  \
      return [](auto e) -> std::string_view {                                                                          \
        std::string_view name{};                                                                                       \
        template for (constexpr std::meta::info en :                                                                   \
                      std::meta::enumerators_of(^^std::remove_cvref_t<decltype(e)>))                                   \
          if (e == [:en:]) name = std::meta::identifier_of(en);                                                        \
        return name;                                                                                                   \
      };                                                                                                               \
    }                                                                                                                  \
    [[nodiscard]] static constexpr auto each_field(auto f) {                                                           \
      return [f]<class X>(X&& x) {                                                                                     \
        template for (constexpr std::meta::info m : std::meta::nonstatic_data_members_of(                              \
                          ^^std::remove_cvref_t<X>, std::meta::access_context::current()))                             \
          f(std::forward<X>(x).[:m:]);                                                                                 \
      };                                                                                                               \
    }
#else
#  define TACIT_REFLECT(Self)
#endif

//  The reusable, vocabulary-independent core: drop into any placeholder struct after its members.
#define TACIT_CORE(Self)                                                                                               \
  using self = Self;                                                                                                   \
  static constexpr bool is_tacit_placeholder = true;                                                                   \
  /* defaulted explicitly: `operator=(self)` below is a user-provided copy assignment, which would                     \
     otherwise DEPRECATE the implicit copy constructor (-Wdeprecated-copy-with-user-provided-copy) */                  \
  constexpr Self() = default;                                                                                          \
  constexpr Self(Self const&) = default;                                                                               \
  TACIT_COMPARE(==) TACIT_COMPARE(!=) TACIT_COMPARE(<) TACIT_COMPARE(>)                                                \
  TACIT_COMPARE(<=) TACIT_COMPARE(>=) TACIT_SECTION(+) TACIT_SECTION(-)                                                \
  TACIT_SECTION(*) TACIT_SECTION(/) TACIT_SECTION(%) TACIT_SECTION(^)                                                  \
  TACIT_SECTION(&) TACIT_SECTION(|) TACIT_SECTION(&&) TACIT_SECTION(||) TACIT_SECTION(<<) TACIT_SECTION(>>)            \
  TACIT_MEMPTR                                                                                                         \
  TACIT_UNARY_AS(*, TACIT_MARK_STAR) TACIT_UNARY(-) TACIT_UNARY(+) TACIT_UNARY(!) TACIT_UNARY(~)                 \
  TACIT_UNARY_AS(&, TACIT_MARK_AMP)                            \
  TACIT_UNARY(++) TACIT_UNARY(--) TACIT_UNARY_POST(++) TACIT_UNARY_POST(--)                                            \
  TACIT_ASSIGN(+=) TACIT_ASSIGN(-=) TACIT_ASSIGN(*=) TACIT_ASSIGN(/=) TACIT_ASSIGN(%=)                                 \
  TACIT_ASSIGN(^=) TACIT_ASSIGN(&=) TACIT_ASSIGN(|=) TACIT_ASSIGN(<<=) TACIT_ASSIGN(>>=)                               \
  template <class Y>                                                                                                   \
    requires tacit::detail::not_fn<Y> && (!std::is_same_v<std::remove_cvref_t<Y>, self>)                               \
  [[nodiscard]] constexpr auto operator=(Y&& y) const {                                                                \
    return tacit::detail::fn{[y = std::forward<Y>(y)](auto&& x) -> decltype(auto)                                      \
             { return std::forward<decltype(x)>(x) = y; }};                                                            \
  }                                                                                                                    \
  /* `_ = _` is two-input, like `_ += _` — the pothole was that only `=` lacked its two-blank                          \
     form. Declaring this suppresses the implicit copy assignment, which a stateless tag never                         \
     needed. */                                                                                                        \
  [[nodiscard]] constexpr auto operator=(self) const {                                                                 \
    return tacit::detail::fn{[](auto&& a, auto&& b) -> decltype(auto)                                                  \
             { return std::forward<decltype(a)>(a) = std::forward<decltype(b)>(b); },                                  \
             tacit::detail::nary{}};                                                                                   \
  }                                                                                                                    \
  /* comma is the tupling section: `_, _` == (a, b) -> {a, b}, `_, y` == x -> {x, y}, and each                         \
     further `,` appends an operand — see `comma_section`. Operands that are an `fn` or an already                     \
     accumulated comma section are excluded here; their own overloads take those. */                                   \
  template <class Y>                                                                                                   \
    requires tacit::detail::not_fn<Y> && tacit::detail::not_comma<Y> &&                                                \
             (!std::is_same_v<std::remove_cvref_t<Y>, self>)                                                           \
  [[nodiscard]] friend constexpr auto operator,(self s, Y y) {                                                         \
    return tacit::detail::make_comma(s, std::move(y));                                                                 \
  }                                                                                                                    \
  template <class X>                                                                                                   \
    requires tacit::detail::not_fn<X> && tacit::detail::not_comma<X> &&                                                \
             (!std::is_same_v<std::remove_cvref_t<X>, self>)                                                           \
  [[nodiscard]] friend constexpr auto operator,(X x, self s) {                                                         \
    return tacit::detail::make_comma(std::move(x), s);                                                                 \
  }                                                                                                                    \
  /* a template, not a plain friend: a non-template body with a deduced return type is compiled                        \
     with the class, which would instantiate comma_section<self, self> — and so std::tuple<_, _> —                     \
     during the header parse, ambiguating the std::tuple specialisation a later include of                             \
     <tacit/experimental/std_blanks.hpp> declares. As a template it is instantiated on use, like                       \
     every other comma overload. */                            \
  template <class = void>                                                                                              \
  [[nodiscard]] friend constexpr auto operator,(self a, self b) {                                                      \
    return tacit::detail::make_comma(a, b);                                                                            \
  }                                                                                                                    \
  [[nodiscard]] static constexpr auto operator()() {                                                                   \
    return tacit::detail::fn{                                                                                          \
        [](auto&& x) -> decltype(auto) { return std::invoke(std::forward<decltype(x)>(x)); }};                         \
  }                                                                                                                    \
  template <class... Y>                                                                                                \
  [[nodiscard]] static constexpr auto operator()(Y&&... ys) {                                                          \
    if constexpr ((tacit::detail::is_slot_v<Y> || ...)) /* `_(_)` == (f, x) -> f(x) */                                 \
      return tacit::detail::make_section(                                                                              \
          [](auto&& f, auto&&... as) -> decltype(auto)                                                                 \
            { return std::invoke(std::forward<decltype(f)>(f),                                                         \
                                 std::forward<decltype(as)>(as)...); },                                                \
          std::forward<Y>(ys)...);                                                                                     \
    else /* an fn like every other builder, so `_(a)` composes and projects — not a bare lambda */                    \
      return tacit::detail::fn{[... ys = std::forward<Y>(ys)](auto&& x, auto&&... zs) -> decltype(auto) {              \
        return std::invoke(std::forward<decltype(x)>(x), ys..., std::forward<decltype(zs)>(zs)...);                    \
      }};                                                                                                              \
  }                                                                                                                    \
  /* `_.get<0>()` and `_.get<int>()` — tuple-like projection. Overloaded on parameter KIND, so the                     \
     index and type forms share the name; both return an `fn`, so they compose like any other verb                     \
     (`_.get<0>().size()`, `sort(v, {}, _.get<1>())`). The plain `_.get()` from the member table is                    \
     untouched: its template parameter is a *type* pack bound to function arguments, so `<0>` is not                   \
     a valid explicit argument for it and `<int>` leaves it wanting a call argument that is not                        \
     there — neither form is viable, and no ambiguity arises. */                                                       \
  template <std::size_t I>                                                                                             \
  [[nodiscard]] static constexpr auto get() {                                                                          \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires tacit::detail::has_get_at<I, X>                                                                  \
             { return tacit::detail::get_<I>(std::forward<X>(x)); }};                                                  \
  }                                                                                                                    \
  template <class T>                                                                                                   \
  [[nodiscard]] static constexpr auto get() {                                                                          \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires tacit::detail::has_get_type<T, X>                                                                \
             { return tacit::detail::get_<T>(std::forward<X>(x)); }};                                                  \
  }                                                                                                                    \
  /* the older spellings, kept as aliases so they gain the free-function route and compose too */                      \
  template <class... Ts>                                                                                               \
  [[nodiscard]] static constexpr auto get_types() { return self::template get<Ts...>(); }                              \
  template <std::size_t I>                                                                                             \
  [[nodiscard]] static constexpr auto get_at() { return self::template get<I>(); }                                     \
  /* the rest of the type-argument family — `_.any_cast<int>()`, `_.duration_cast<seconds>()`, … */                    \
  TACIT_STD_TFREES(TACIT_TFREE1)                                                                                       \
  TACIT_TO_MEMBERS                                                                                                     \
  template <class I>                                                                                                   \
  [[nodiscard]] constexpr auto operator[](I&& i) const {                                                               \
    if constexpr (tacit::detail::is_slot_v<I>) /* `_[_]` == (x, i) -> x[i] */                                          \
      return tacit::detail::make_section(                                                                              \
          [](auto&& x, auto&& j) -> decltype(auto)                                                                     \
            { return std::forward<decltype(x)>(x)[std::forward<decltype(j)>(j)]; },                                    \
          std::forward<I>(i));                                                                                         \
    else                                                                                                               \
      return tacit::detail::fn{[i = std::forward<I>(i)](auto&& x) -> decltype(auto)                                    \
               { return std::forward<decltype(x)>(x)[i]; }};                                                           \
  }                                                                                                                    \
  TACIT_REFLECT(Self)                                                                                                  \
  static_assert(true)

// The standard-library vocabulary: one editable table, grouped by role with line breaks. A derived placeholder pulls it
// all with TACIT_STD_MEMBERS(TACIT_MEMBER), or just lists the names it wants.
#define TACIT_STD_MEMBERS(X)                                                                                           \
  /* access   */ X(at); X(front); X(back); X(top);                                                                     \
  /* capacity */ X(length); X(capacity); X(reserve); X(resize); X(shrink_to_fit); X(max_size);                         \
                 X(size_bytes);                                                                                        \
  /* modify   */ X(clear); X(push_back); X(pop_back); X(push_front); X(pop_front); X(push); X(pop);                    \
                 X(emplace); X(emplace_back); X(emplace_front); X(emplace_hint); X(insert);                            \
                 X(insert_or_assign); X(try_emplace); X(erase); X(extract); X(remove); X(remove_if);                   \
                 X(splice); X(merge); X(unique); X(sort); X(append); X(assign); X(replace);                            \
                 X(before_begin); X(insert_after); X(emplace_after); X(erase_after);                                   \
  /* range23  */ X(assign_range); X(append_range); X(insert_range); X(prepend_range); X(push_range);                   \
  /* lookup   */ X(find); X(count); X(contains); X(lower_bound); X(upper_bound); X(equal_range);                       \
  /* string   */ X(substr); X(compare); X(starts_with); X(ends_with); X(rfind); X(find_first_of);                      \
                 X(find_last_of); X(find_first_not_of); X(find_last_not_of); X(c_str); X(str);                         \
                 X(remove_prefix); X(remove_suffix); X(to_string);                                                     \
  /* optional */ X(has_value); X(value); X(value_or); X(and_then); X(transform); X(or_else); X(error);                 \
                 X(error_or); X(transform_error); X(index); X(reset);                                                  \
  /* pointer  */ X(get); X(release); X(use_count); X(expired); X(lock); X(owner_before);                               \
  /* error    */ X(what); X(code); X(message); X(category); X(name);                                                   \
  /* path     */ X(filename); X(stem); X(extension); X(parent_path); X(relative_path); X(root_path);                   \
                 X(has_filename); X(has_extension); X(is_absolute); X(is_relative);                                    \
                 X(replace_extension); X(replace_filename); X(remove_filename);                                        \
                 X(lexically_normal); X(string); X(generic_string); X(native);                                         \
  /* stream   */ X(flush); X(is_open); X(open); X(close); X(good); X(eof); X(fail); X(bad); X(rdbuf);                  \
  /* thread   */ X(join); X(detach); X(joinable); X(wait); X(valid); X(share); X(load); X(store);                      \
                 X(exchange); X(fetch_add); X(fetch_sub); X(unlock); X(try_lock); X(notify_one);                       \
                 X(notify_all); X(request_stop); X(stop_requested);                                                    \
  /* numeric  */ X(real); X(imag); X(time_since_epoch);                                                                \
  /* bitset   */ X(test); X(set); X(flip); X(all); X(any); X(none);                                                    \
  /* view     */ X(base); X(subspan);                                                                                  \
  /* regex    */ X(position); X(prefix); X(suffix); X(ready);                                                          \
  /* source   */ X(file_name); X(function_name); X(line); X(column);

//  Free-function forwarder: the third dispatch kind, beside member calls and the range CPOs. Some
//  vocabulary is spelled as a free function in the standard library and can never be a member — a
//  bare `-42` has no members at all — so those names route to `std::` instead of to `x.NAME(...)`.
#define TACIT_FREE1(NAME, FN)                                                                                          \
  [[nodiscard]] static constexpr auto NAME() {                                                                         \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires requires(X&& xx) { FN(xx); } { return FN(std::forward<X>(x)); }};                                \
  }

//  The same, for a free function whose argument is a TYPE rather than a value: `_.any_cast<int>()`.
//  The subject is still the one call argument; the type rides in the template list.
// `_.to<std::vector>()` — C++23 `ranges::to`, the pipeline terminator. Both kinds of argument: a TEMPLATE
// (`to<std::vector>`, element deduced) and a type (`to<std::vector<int>>`), the same kind-overload that lets `get<0>`
// and `get<int>` share a name. Qualified rather than reached by ADL: `to` lives in `std::ranges`, which a `std::vector`
// argument does not associate.
#if TACIT_HAS_RANGES_TO
#define TACIT_TO_MEMBERS                                                                                               \
  template <template <class...> class C>                                                                               \
  [[nodiscard]] static constexpr auto to() {                                                                           \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires requires(X&& xx) { std::ranges::to<C>(std::forward<X>(xx)); }                                    \
             { return std::ranges::to<C>(std::forward<X>(x)); }};                                                      \
  }                                                                                                                    \
  template <class C>                                                                                                   \
  [[nodiscard]] static constexpr auto to() {                                                                           \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires requires(X&& xx) { std::ranges::to<C>(std::forward<X>(xx)); }                                    \
             { return std::ranges::to<C>(std::forward<X>(x)); }};                                                      \
  }
#else
#define TACIT_TO_MEMBERS
#endif

#define TACIT_TFREE1(NAME)                                                                                             \
  template <class T>                                                                                                   \
  [[nodiscard]] static constexpr auto NAME() {                                                                         \
    return tacit::detail::fn{[]<class X>(X&& x) -> decltype(auto)                                                      \
             requires tacit::detail::has_##NAME<T, X>                                                                  \
             { return tacit::detail::NAME##_<T>(std::forward<X>(x)); }};                                               \
  }

//  The free-function table: names that are free functions in std, kept small and numeric-leaning.
#define TACIT_STD_FREES(X)                                                                                             \
  X(abs, std::abs)     X(sqrt, std::sqrt)   X(cbrt, std::cbrt)   X(floor, std::floor)                                  \
  X(ceil, std::ceil)   X(round, std::round) X(trunc, std::trunc) X(exp, std::exp)                                      \
  X(log, std::log)     X(log2, std::log2)   X(log10, std::log10) X(signbit, std::signbit)                              \
  X(isnan, std::isnan) X(isinf, std::isinf) X(isfinite, std::isfinite)

#define TACIT_STD_CPOS1(X)                                                                                             \
  X(begin,  std::ranges::begin)  X(end,   std::ranges::end)                                                            \
  X(cbegin, std::ranges::cbegin) X(cend,  std::ranges::cend)                                                           \
  X(rbegin, std::ranges::rbegin) X(rend,  std::ranges::rend)                                                           \
  X(crbegin, std::ranges::crbegin) X(crend, std::ranges::crend)                                                        \
  X(size,   std::ranges::size)   X(ssize, std::ranges::ssize)                                                          \
  X(empty,  std::ranges::empty)  X(data,  std::ranges::data)                                                           \
  X(cdata,  std::ranges::cdata)
// clang-format on

// Additive extension hook for the one `_`: pre-#define a comma list of bare member-call names before including, and
// each becomes a first-class *verb* on `_` — reachable as `_.name(...)`, composing and blank-taking like the built-in
// vocabulary (`_.name(_)`), on `_` and its projections alike:
//   #define TACIT_VERBS area, perimeter, scale
// This is how you teach `_` a domain vocabulary — there is no separate placeholder object; `_` stays the one interface.
// (Its type-level twin is TACIT_NOUNS, below.) VOCABULARY FILE (the richer twin of the TACIT_VERBS / TACIT_NOUNS comma
// lists). Point TACIT_VOCABULARY at a header, or just drop a `tacit_vocabulary.hpp` on the include path and it is found
// by `__has_include`:
//
//     #define TACIT_VOCABULARY <bank/vocabulary.hpp>
//     #include <tacit/_.hpp>
//
// The file is a list of entries, and — unlike a bare comma list — each entry chooses its *dispatch kind*, so a domain
// free function or customization point is reachable, not just member calls:
//
//     TACIT_VERB(deposit)               // x.deposit(a...)
//     TACIT_FREE(area, geom::area)      // geom::area(x)          — a bare value has no members
//     TACIT_CPO(extent, geom::extent)   // geom::extent(x)        — a customization point
//     TACIT_NOUN(payload_type)          // _::payload_type::of<X> == X::payload_type
//
// The file is included ONCE PER SURFACE (`_`, its projections, the arrow proxy, comma sections, the lift), each time
// with the entry macros bound to that surface's forwarder — the X-macro pattern. So it must NOT have an include guard
// or `#pragma once`, and must contain nothing but entries.
//
// Two entries, one list, one place: every translation unit that includes tacit sees the same vocabulary by
// construction. That is the point over `#define TACIT_VERBS`, which every TU has to repeat identically before the
// include or else end up with a different `_`. It is not a *guarantee* — a TU that points TACIT_VOCABULARY somewhere
// else still gets a different `_`, and mixing those in one program is an ODR violation like any other — but it removes
// the per-TU ritual that made the mismatch easy to cause by accident.
#if defined(TACIT_VOCABULARY)
#define TACIT_VOCABULARY_FILE TACIT_VOCABULARY
#elif defined(__has_include)
#if __has_include(<tacit_vocabulary.hpp>)
#define TACIT_VOCABULARY_FILE <tacit_vocabulary.hpp>
#endif
#endif

#ifndef TACIT_VERBS
#define TACIT_VERBS
#endif

// ------------------------------------------------------------------------------------------------
//  Type-level projection vocabulary: the twin of the value-level member table above, but for types.
//  Each entry is a metafunction living inside `struct _`, pulling a nested member out of a
//  later-supplied type X and applied as `_::name::of<X>`. TACIT_TYPE_MEMBER projects a nested
//  type/alias (`X::name`); TACIT_TYPE_TEMPLATE projects a nested *template* (`X::template name<A...>`,
//  the rebind family), reached as `_::name<A...>::of<X>` — you supply the template's own arguments
//  before applying. `of` is the applier (distinct from bind's blank-filling `with`).
#define TACIT_TYPE_MEMBER(NAME)                                                                                        \
  struct NAME {                                                                                                        \
    template <class X> using of = typename X::NAME;                                                                    \
  };                                                                                                                   \
  static_assert(true)
#define TACIT_TYPE_TEMPLATE(NAME)                                                                                      \
  template <class... A> struct NAME {                                                                                  \
    template <class X> using of = typename X::template NAME<A...>;                                                     \
  };                                                                                                                   \
  static_assert(true)

// clang-format off
//  The standard nested-type vocabulary (one editable table, grouped by role). Every name here is a
//  nested type some standard component exposes; a projection instantiates only on use, so listing a
//  name a given X lacks costs nothing until `_::name::of<X>` is actually asked for.
#define TACIT_STD_TYPE_MEMBERS(X)                                                                                      \
  /* element */ X(value_type); X(element_type); X(reference); X(const_reference);                                      \
                X(pointer); X(const_pointer);                                                                          \
  /* size    */ X(size_type); X(difference_type);                                                                      \
  /* iterate */ X(iterator); X(const_iterator); X(reverse_iterator); X(const_reverse_iterator);                        \
                X(iterator_category); X(iterator_concept);                                                             \
                X(local_iterator); X(const_local_iterator);                                                            \
  /* assoc   */ X(key_type); X(mapped_type); X(key_compare); X(value_compare);                                         \
                X(hasher); X(key_equal); X(node_type); X(insert_return_type);                                          \
  /* alloc   */ X(allocator_type); X(void_pointer); X(const_void_pointer);                                             \
  /* pair    */ X(first_type); X(second_type);                                                                         \
  /* traits  */ X(char_type); X(traits_type); X(int_type);                                                             \
                X(pos_type); X(off_type); X(state_type); X(string_type);                                               \
  /* monad   */ X(error_type); X(unexpected_type); X(result_type);                                                     \
  /* pointer */ X(deleter_type); X(weak_type);                                                                         \
  /* time    */ X(rep); X(period); X(duration); X(time_point); X(clock);                                               \
  /* adaptor */ X(container_type);                                                                                     \
  /* thread  */ X(native_handle_type); X(id);                                                                          \
  /* meta    */ X(type);
// clang-format on

//  Additive extension hook for the type-level `_`: the twin of TACIT_VERBS, a comma list of bare
//  nested-type names, each a first-class *noun* projected as `_.name` — reached as `_::name::of<X>`:
//    #define TACIT_NOUNS shape_tag, payload_type
//  TACIT_NOUN_TEMPLATES is the parameterized variant, for nested *templates* (`X::name<A...>`, the
//  rebind family), reached as `_::name<A...>::of<X>`. Kept a separate list so TACIT_NOUNS stays a
//  clean bare-name list; use it only when you actually project a nested template:
//    #define TACIT_NOUN_TEMPLATES rebind
#ifndef TACIT_NOUNS
#define TACIT_NOUNS
#endif
#ifndef TACIT_NOUN_TEMPLATES
#define TACIT_NOUN_TEMPLATES
#endif

// clang-format off
namespace detail {
// The composable projection wrapper (forward-declared above). Beyond call, subscript, and the operator sections, it
// carries the same std vocabulary as `_` — but every member COMPOSES through the wrapped projection, so
// `_.front().size()` == `x -> size(front(x))`: that is member chaining.
template <class F, class Last> struct fn {
  // `[[no_unique_address]]` so a closure built purely from `_` is genuinely EMPTY, not merely small. `_ > _` wraps a
  // captureless lambda, and an empty `fn` means `std::set<int, decltype(_ > _)>` pays nothing for its comparator — the
  // container's empty-base optimisation applies exactly as it does for `std::greater<>`. Without this the two members
  // would occupy a byte each and every such container would grow.
  [[no_unique_address]] F f;
  // Chain state: `nochain` for every projection that is not a comparison section. See "comparison chains" above —
  // `last` recovers the rightmost operand of the comparison this `fn` represents.
  [[no_unique_address]] Last last{}; // (the initializer keeps plain `fn{f}` warning-clean)
  static constexpr bool chained = !std::is_same_v<Last, nochain> && !std::is_same_v<Last, nary>;

  template <class... A>
    requires requires(F const &g, A &&...a) { g(std::forward<A>(a)...); }
  constexpr decltype(auto) operator()(A &&...a) const { return f(std::forward<A>(a)...); }
  template <class I> [[nodiscard]] constexpr auto operator[](I i) const {
    return tacit::detail::fn{[g = *this, i = std::move(i)](auto &&...x) -> decltype(auto) {
      return g(std::forward<decltype(x)>(x)...)[i];
    }};
  }
  // Operator sections as hidden friends — found by ADL, including across a module boundary (`import tacit;`): `g op
  // value` / `value op g` compose to a unary fn; `g op h` is binary.
#define TACIT_FN_OP(op)                                                                                                \
  template <not_fn Y> [[nodiscard]] friend constexpr auto operator op(fn g, Y y) {                                     \
    if constexpr (is_blank_v<Y>) /* `g op _` is two-input, like `_ op _` — not a bound value */                        \
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {                                             \
        return g(std::forward<decltype(a)>(a)) op std::forward<decltype(b)>(b); }, nary{}};                            \
    else                                                                                                               \
      return tacit::detail::fn{                                                                                        \
        [g, y](auto &&...x) -> decltype(auto) { return g(std::forward<decltype(x)>(x)...) op y; }};                    \
  }                                                                                                                    \
  template <not_fn X> [[nodiscard]] friend constexpr auto operator op(X &&x, fn g) {                                   \
    if constexpr (is_blank_v<std::remove_cvref_t<X>>) /* `_ op g`, the mirror */                                       \
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {                                             \
        return std::forward<decltype(a)>(a) op g(std::forward<decltype(b)>(b)); }, nary{}};                            \
    else if constexpr (std::is_copy_constructible_v<std::remove_cvref_t<X>>)                                            \
      return tacit::detail::fn{                                                                                        \
          [g, x = std::forward<X>(x)](auto &&...y) -> decltype(auto) {                                                 \
            return x op g(std::forward<decltype(y)>(y)...); }};                                                        \
    else { /* non-copyable LVALUE (stream): by reference, e.g. cout << _.size(); rvalues: rejected */                  \
      static_assert(std::is_lvalue_reference_v<X>,                                                                     \
                    "binding a non-copyable temporary would dangle: name it first");                                   \
      return tacit::detail::fn{                                                                                        \
          [g, &x](auto &&...y) -> decltype(auto) { return x op g(std::forward<decltype(y)>(y)...); }};                 \
    }                                                                                                                  \
  }                                                                                                                    \
  template <class G, class L> [[nodiscard]] friend constexpr auto operator op(fn g, fn<G, L> h) {                      \
    return tacit::detail::fn{[g, h](auto &&a, auto &&b) -> decltype(auto) {                                            \
      return g(std::forward<decltype(a)>(a)) op h(std::forward<decltype(b)>(b));                                       \
    }, nary{}};                                                                                                        \
  }
  TACIT_FN_OP(+) TACIT_FN_OP(-) TACIT_FN_OP(*) TACIT_FN_OP(/) TACIT_FN_OP(%) TACIT_FN_OP(^)
  TACIT_FN_OP(&) TACIT_FN_OP(|) TACIT_FN_OP(&&) TACIT_FN_OP(||) TACIT_FN_OP(<<) TACIT_FN_OP(>>)
#undef TACIT_FN_OP
  // `->*` through a projection keeps its natural meaning too (via `memsel`, like the blank's
  // section): `_.front() ->* &W::x` == `xs -> (*front(xs)).x`.
  template <not_fn Y> [[nodiscard]] friend constexpr auto operator->*(fn g, Y y) {
    if constexpr (is_blank_v<Y>) /* `g ->* _` is two-input, like `_ ->* _` */
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {
        return memsel(g(std::forward<decltype(a)>(a)), std::forward<decltype(b)>(b)); }, nary{}};
    else
      return tacit::detail::fn{
        [g, y](auto &&...x) -> decltype(auto) { return memsel(g(std::forward<decltype(x)>(x)...), y); }};
  }
  template <not_fn X> [[nodiscard]] friend constexpr auto operator->*(X &&x, fn g) {
    if constexpr (is_blank_v<std::remove_cvref_t<X>>) /* `_ ->* g`, the mirror */
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {
        return memsel(std::forward<decltype(a)>(a), g(std::forward<decltype(b)>(b))); }, nary{}};
    else
      return tacit::detail::fn{[g, x = std::forward<X>(x)](auto &&...y) -> decltype(auto) {
        return memsel(x, g(std::forward<decltype(y)>(y)...)); }};
  }
  template <class G, class L> [[nodiscard]] friend constexpr auto operator->*(fn g, fn<G, L> h) {
    return tacit::detail::fn{[g, h](auto &&a, auto &&b) -> decltype(auto) {
      return memsel(g(std::forward<decltype(a)>(a)), h(std::forward<decltype(b)>(b)));
    }, nary{}};
  }
  // The six comparisons are TACIT_FN_OP plus chain state, so `0 < _ < 10` means what it says: when the left operand is
  // itself a comparison section, the new one folds into a conjunction against that section's rightmost operand rather
  // than against its bool RESULT. Every link records its own rightmost operand, so chains of any length and any mix of
  // the six operators compose.
#define TACIT_FN_COMPARE(op)                                                                                           \
  template <not_fn Y> [[nodiscard]] friend constexpr auto operator op(fn g, Y y) {                                     \
    if constexpr (is_blank_v<Y>) /* `g op _` is the two-input comparator, like `_ op _` */                             \
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {                                             \
        return g(std::forward<decltype(a)>(a)) op std::forward<decltype(b)>(b); }, nary{}};                            \
    else if constexpr (fn::chained) { /* `(… op0 m) op y` == `(… op0 m) && (m op y)` */                                \
      /* Folding against a bool is never what the spelling means: `(_ < 10) == false` would become                     \
         `(x < 10) && (10 == false)` — an ALWAYS-FALSE closure, when the writer meant negation.                        \
         Rejected here rather than documented as a gotcha; the fix is spelled in the message. */                       \
      static_assert(!std::is_same_v<std::remove_cvref_t<Y>, bool>,                                                     \
                    "a comparison chain folded against a bool: `(_ < 10) == false` means "                             \
                    "`(x < 10) && (10 == false)`, not negation — write `_ >= 10`, or negate with `!`");                \
      return tacit::detail::fn{[g, y](auto &&...x) -> bool {                                                           \
                                 return static_cast<bool>(g(x...)) &&                                                  \
                                        static_cast<bool>(g.last(x...) op y);                                          \
                               },                                                                                      \
                               tacit::detail::always{y}};                                                              \
    }                                                                                                                  \
    else /* first link: unchanged behaviour, plus the state a following comparison folds against */                    \
      return tacit::detail::fn{                                                                                        \
          [g, y](auto &&...x) -> decltype(auto) { return g(std::forward<decltype(x)>(x)...) op y; },                   \
          tacit::detail::always{y}};                                                                                   \
  }                                                                                                                    \
  template <not_fn X> [[nodiscard]] friend constexpr auto operator op(X &&x, fn g) {                                   \
    if constexpr (is_blank_v<std::remove_cvref_t<X>>) /* `_ op g`, the mirror */                                       \
      return tacit::detail::fn{[g](auto &&a, auto &&b) -> decltype(auto) {                                             \
        return std::forward<decltype(a)>(a) op g(std::forward<decltype(b)>(b)); }, nary{}};                            \
    else if constexpr (std::is_copy_constructible_v<std::remove_cvref_t<X>>)                                            \
      return tacit::detail::fn{                                                                                        \
          [g, x = std::forward<X>(x)](auto &&...y) -> decltype(auto) {                                                 \
            return x op g(std::forward<decltype(y)>(y)...); }, g}; /* rightmost operand: g */                          \
    else { /* non-copyable LVALUE (stream): by reference; rvalues: rejected */                                         \
      static_assert(std::is_lvalue_reference_v<X>,                                                                     \
                    "binding a non-copyable temporary would dangle: name it first");                                   \
      return tacit::detail::fn{                                                                                        \
          [g, &x](auto &&...y) -> decltype(auto) { return x op g(std::forward<decltype(y)>(y)...); }, g};              \
    }                                                                                                                  \
  }                                                                                                                    \
  template <class G, class L> [[nodiscard]] friend constexpr auto operator op(fn g, fn<G, L> h) {                      \
    return tacit::detail::fn{[g, h](auto &&a, auto &&b) -> decltype(auto) {                                            \
      return g(std::forward<decltype(a)>(a)) op h(std::forward<decltype(b)>(b));                                       \
    }, nary{}};                                                                                                        \
  }
  TACIT_FN_COMPARE(==) TACIT_FN_COMPARE(!=) TACIT_FN_COMPARE(<)
  TACIT_FN_COMPARE(>)  TACIT_FN_COMPARE(<=) TACIT_FN_COMPARE(>=)
#undef TACIT_FN_COMPARE
  // Comma: a projection joins a comma section as an operand like any other, so `(_.size(), _.front())` is `(a, b) ->
  // {size(a), front(b)}`. Without these the built-in comma applies instead — it evaluates and DISCARDS the left
  // operand, silently yielding just the right one. (`[[nodiscard]]` on the vocabulary makes that a warning, not an
  // error; a section that quietly loses half of what you wrote is worth an overload.)
  template <class Y>
    requires(not_fn<Y> && not_comma<Y>)
  [[nodiscard]] friend constexpr auto operator,(fn g, Y y) {
    return tacit::detail::make_comma(g, std::move(y));
  }
  template <class X>
    requires(not_fn<X> && not_comma<X>)
  [[nodiscard]] friend constexpr auto operator,(X x, fn g) {
    return tacit::detail::make_comma(std::move(x), g);
  }
  template <class G, class L> [[nodiscard]] friend constexpr auto operator,(fn g, fn<G, L> h) {
    return tacit::detail::make_comma(g, h);
  }
  // Unary operators compose through the projection: `op g` == x -> op g(x).
#define TACIT_FN_UNARY_AS(op, WRAP)                                                                                    \
  [[nodiscard]] friend constexpr auto operator op(fn g) {                                                              \
    [[maybe_unused]] auto tacit_mark_inner_ = g;                                                                        \
    return WRAP([g](auto &&...x) -> decltype(auto) { return op g(std::forward<decltype(x)>(x)...); });                 \
  }
#define TACIT_FN_UNARY(op) TACIT_FN_UNARY_AS(op, TACIT_PLAIN)
#define TACIT_FN_UNARY_POST(op)                                                                                        \
  [[nodiscard]] friend constexpr auto operator op(fn g, int) {                                                         \
    return tacit::detail::fn{                                                                                          \
        [g](auto &&...x) -> decltype(auto) { return g(std::forward<decltype(x)>(x)...) op; }};                         \
  }
  TACIT_FN_UNARY_AS(*, TACIT_MARK_STAR) TACIT_FN_UNARY(-) TACIT_FN_UNARY(+) TACIT_FN_UNARY(!)
  TACIT_FN_UNARY(~) TACIT_FN_UNARY_AS(&, TACIT_MARK_AMP)
  TACIT_FN_UNARY(++) TACIT_FN_UNARY(--) TACIT_FN_UNARY_POST(++) TACIT_FN_UNARY_POST(--)
#undef TACIT_FN_UNARY
#undef TACIT_FN_UNARY_POST
  // (`|` is a bitwise section like `&`, above — general composition is `tacit::compose`, not `f | g`.) Vocabulary —
  // each name composes through the projection (member chaining). No-blank args only.
#define TACIT_FN_MEMBER(NAME)                                                                                          \
  template <class... A>                                                                                                \
    requires(!(tacit::detail::is_blank_v<A> || ...)) /* see "no dead closures" below */                                \
  [[nodiscard]] constexpr auto NAME(A &&...a) const {                                                                  \
    return tacit::detail::fn{                                                                                          \
        [g = *this, ... a = std::forward<A>(a)]<class... X>(X &&...x) -> decltype(auto)                                \
          requires requires(X &&...xx, std::decay_t<A> const &...aa) { /* guard == body */                             \
            std::declval<fn const &>()(std::forward<X>(xx)...).NAME(aa...);                                            \
          } { return g(std::forward<X>(x)...).NAME(a...); }};                                                          \
  }                                                                                                                    \
  static_assert(true)
#define TACIT_FN_CPO1(NAME, CPO)                                                                                       \
  [[nodiscard]] constexpr auto NAME() const {                                                                          \
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)                                       \
             requires requires(X &&...xx) { CPO(std::declval<fn const &>()(std::forward<X>(xx)...)); }                 \
             { return CPO(g(std::forward<X>(x)...)); }};                                                               \
  }
  // `._()` — the application form, composed. On `_` itself, application is spelled `_(a, b)` (`operator()` builds `f ->
  // f(a, b)`); on a projection that spelling is taken, since calling an `fn` means "call this closure". So the
  // vocabulary carries it under the placeholder's own name: `_.x()._()` invokes what `_.x()` produced, and `._(a, b)`
  // calls it with those arguments — `x -> invoke(x.x(), a, b)`. Returns an `fn`, so a callable-returning chain keeps
  // going.
  template <class... Y>
    requires(!(tacit::detail::is_blank_v<Y> || ...))
  [[nodiscard]] constexpr auto _(Y &&...ys) const {
    return tacit::detail::fn{
        [g = *this, ... ys = std::forward<Y>(ys)]<class... X>(X &&...x) -> decltype(auto)
          requires requires(X &&...xx, Y &&...yy) {
            std::invoke(std::declval<fn const &>()(std::forward<X>(xx)...), std::forward<Y>(yy)...);
          } { return std::invoke(g(std::forward<X>(x)...), ys...); }};
  }
  TACIT_STD_MEMBERS(TACIT_FN_MEMBER)
  TACIT_FOR_EACH(TACIT_FN_MEMBER, TACIT_VERBS)
  TACIT_STD_CPOS1(TACIT_FN_CPO1)
#define TACIT_FN_FREE1(NAME, FN)                                                                                       \
  [[nodiscard]] constexpr auto NAME() const {                                                                          \
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)                                       \
             requires requires(X &&...xx) { FN(std::declval<fn const &>()(std::forward<X>(xx)...)); }                  \
             { return FN(g(std::forward<X>(x)...)); }};                                                                \
  }
  TACIT_STD_FREES(TACIT_FN_FREE1)
#define TACIT_FN_TFREE1(NAME)                                                                                          \
  template <class T>                                                                                                   \
  [[nodiscard]] constexpr auto NAME() const {                                                                          \
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)                                       \
             requires tacit::detail::has_##NAME<                                                                       \
                 T, decltype(std::declval<fn const &>()(std::forward<X>(x)...))>                                       \
             { return tacit::detail::NAME##_<T>(g(std::forward<X>(x)...)); }};                                         \
  }
  TACIT_STD_TFREES(TACIT_FN_TFREE1)
  // tuple-like projection, composed: `_.front().get<0>()`. Both kinds, as on `_`.
  template <std::size_t I>
  [[nodiscard]] constexpr auto get() const {
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)
             requires tacit::detail::has_get_at<
                 I, decltype(std::declval<fn const &>()(std::forward<X>(x)...))>
             { return tacit::detail::get_<I>(g(std::forward<X>(x)...)); }};
  }
  template <class T>
  [[nodiscard]] constexpr auto get() const {
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)
             requires tacit::detail::has_get_type<
                 T, decltype(std::declval<fn const &>()(std::forward<X>(x)...))>
             { return tacit::detail::get_<T>(g(std::forward<X>(x)...)); }};
  }
#if TACIT_HAS_RANGES_TO
  template <template <class...> class C>
  [[nodiscard]] constexpr auto to() const {
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)
             requires requires(X &&...xx) {
               std::ranges::to<C>(std::declval<fn const &>()(std::forward<X>(xx)...));
             } { return std::ranges::to<C>(g(std::forward<X>(x)...)); }};
  }
  template <class C>
  [[nodiscard]] constexpr auto to() const {
    return tacit::detail::fn{[g = *this]<class... X>(X &&...x) -> decltype(auto)
             requires requires(X &&...xx) {
               std::ranges::to<C>(std::declval<fn const &>()(std::forward<X>(xx)...));
             } { return std::ranges::to<C>(g(std::forward<X>(x)...)); }};
  }
#endif
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_FN_MEMBER(NAME);
#define TACIT_FREE(NAME, FN) TACIT_FN_FREE1(NAME, FN)
#define TACIT_CPO(NAME, FN) TACIT_FN_CPO1(NAME, FN)
#define TACIT_NOUN(NAME)
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
#undef TACIT_FN_FREE1
#undef TACIT_FN_TFREE1
  // Range-adaptor verbs compose through the projection, so a pipeline keeps chaining (opt-in).
#ifdef TACIT_VIEWS
#define TACIT_FN_VIEW(NAME, ADAPT)                                                                                     \
  template <class... A> [[nodiscard]] constexpr auto NAME(A&&... a) const {                                            \
    return tacit::detail::fn{                                                                                          \
        [g = *this, ... a = std::forward<A>(a)]<class X>(X&& x) -> decltype(auto)                                      \
          requires requires(X&& xx, std::decay_t<A> const&... aa) { /* guard == body */                                \
            std::views::ADAPT(std::declval<fn const&>()(std::forward<X>(xx)), aa...);                                  \
          } { return std::views::ADAPT(g(std::forward<X>(x)), a...); }};                                               \
  }
  TACIT_VIEW_VERBS(TACIT_FN_VIEW)
#undef TACIT_FN_VIEW
#endif
#undef TACIT_FN_MEMBER
#undef TACIT_FN_CPO1
};
// `fn{f}` is an unchained projection; `fn{f, last}` a comparison section. Spelled out (rather than left to aggregate
// deduction) so the one-argument form unambiguously picks up the `nochain` default.
template <class F> fn(F) -> fn<F, nochain>;

// ------------------------------------------------------------------------------------------------
// SYNTHETIC SIGILS (opt-in: `#define TACIT_SIGILS`).
//
//     f >>* g      compose, left to right   x -> g(f(x))
//     f <<* g      compose, right to left   x -> f(g(x))
//     f &&& g      fanout                   x -> {f(x), g(x)}
//     f *** g      product                  (a, b) -> {f(a), g(b)}   (or one pair in)
//
// C++ has a closed set of overloadable operators and no way to add to it, so none of these is an
// operator. Each is a TOKEN SEQUENCE that the lexer splits, by maximal munch, into operators that do
// exist — `f &&& g` is binary `&&` applied to `f` and unary `&` applied to `g`. The sigil is one
// glyph to a reader and two operators to the compiler, and that is the whole trick.
//
// The catch is that both halves are already spoken for: `f && g` is the logical-and section and `&g`
// is the address-of section. So a sigil cannot be *added* to the surface; it can only be carved out
// of what those two already mean. `marked` is how: under the gate, unary `&` and `*` on a closure
// return a type that DERIVES from `fn`, so every existing use is bit-for-bit unchanged — it calls
// the same way, composes the same way, and the operator sections still find it through its base —
// while carrying a tag the sigil overloads can dispatch on.
//
// What it costs is ONE READING PER SIGIL — the closure-against-marked-closure form of its binary
// half:
//
//     f && (&g)    was and-of-address    now fanout
//     f << (*g)    was shift-of-deref    now compose (and `f >> (*g)` its mirror)
//     f * (**g)    was times-of-deref²   now product
//
// Nothing else moves: the unary sections keep their meaning (the marker derives from `fn`), and a
// non-closure left operand still takes the old reading (`5 && (&g)` binds 5 into the and-section,
// because the sigil overloads require `closure_like` on the left). The readings given up are ones
// nobody writes — shifting by a dereference closure, multiplying by a double-deref — which is why
// this is affordable at all; but it is a real trade, and it is why the whole thing is gated.
//
// The exhaustive sweep behind the choice of these four is in tacit_extras.md: of 391 lexically valid
// two- and three-operator sigils, 368 compile. Haskell's arrow spellings `&&&`, `***` and `+++`
// survive C++'s lexer; `|||`, `>>>` and `<<<` do not (`>>>` lexes as `>>` `>`, and `> g` is not an
// expression). Composition is therefore the mirrored pair `>>*` / `<<*`: both directions from the
// one `*` marker already paid for, arrows pointing the way the data flows. (`->*` is deliberately
// NOT here — it is a real operator with a real job, the ungated member-pointer projection above.)
#ifdef TACIT_SIGILS
struct amp_tag {};   // from unary `&` — the fanout half of `&&&`
struct star_tag {};  // from unary `*` — the compose half of `<<*`
struct star2_tag {}; // from `**`      — the product half of `***`

// A marked closure IS an `fn` (public base, no members of its own), so it inherits the call
// operator, the vocabulary and every hidden-friend section; only its TYPE differs.
// A marked closure IS an `fn` — it derives from one, and that base is the ORIGINAL meaning of the
// unary operator (`&g` still computes `x -> &g(x)`), so nothing existing shifts. Alongside it the
// marker keeps `inner`: the operand the sigil actually wants, un-addressed and un-dereferenced.
// Both are needed, because `f &&& g` must read `g` as itself while `(&g)(x)` must still take an
// address.
template <class Tag, class F, class G> struct marked : fn<F, nochain> {
  [[no_unique_address]] G inner;
};
template <class Tag, class G, class F> [[nodiscard]] constexpr auto mark(G inner, F f) {
  return marked<Tag, F, G>{{f}, inner};
}

template <class T> constexpr bool is_marked_v = false;
template <class Tag, class F, class G> constexpr bool is_marked_v<marked<Tag, F, G>> = true;
template <class T>
concept closure_like = is_fn_v<std::remove_cvref_t<T>> || is_blank_v<T>;

// `**g` — the outer `*` on an already-marked `*g`. Exact match beats the derived-to-base conversion
// the hidden friend `operator*(fn)` would need, so this wins without ambiguity; `inner` rides along.
template <class F, class G> [[nodiscard]] constexpr auto operator*(marked<star_tag, F, G> g) {
  return mark<star2_tag>(g.inner, [g](auto &&...x) -> decltype(auto) {
    return *static_cast<fn<F, nochain> const &>(g)(std::forward<decltype(x)>(x)...);
  });
}

// `f &&& g` == `f && (&g)` — fanout: one input, both closures, a pair out.
template <closure_like L, class F, class G>
[[nodiscard]] constexpr auto operator&&(L f, marked<amp_tag, F, G> g) {
  return fn{[f, h = g.inner](auto &&...x) -> decltype(auto) {
    return std::pair{f(std::forward<decltype(x)>(x)...), h(std::forward<decltype(x)>(x)...)};
  }};
}

// `f <<* g` == `f << (*g)` — compose, right to left: `x -> f(g(x))`. The arrow of `>>*` runs the
// other way, so the two spell both directions from the same `*` marker.
template <closure_like L, class F, class G>
[[nodiscard]] constexpr auto operator<<(L f, marked<star_tag, F, G> g) {
  return fn{[f, h = g.inner](auto &&...x) -> decltype(auto) {
    return f(h(std::forward<decltype(x)>(x)...));
  }};
}

// `f >>* g` == `f >> (*g)` — compose, left to right: `x -> g(f(x))`.
template <closure_like L, class F, class G>
[[nodiscard]] constexpr auto operator>>(L f, marked<star_tag, F, G> g) {
  return fn{[f, h = g.inner](auto &&...x) -> decltype(auto) {
    return h(f(std::forward<decltype(x)>(x)...));
  }};
}

// `f *** g` == `f * (*(*g))` — product: each side of a pair through its own closure. Takes two
// arguments or one pair, so it drops straight into a `transform` over a map.
template <closure_like L, class F, class G>
[[nodiscard]] constexpr auto operator*(L f, marked<star2_tag, F, G> g) {
  return fn{[f, h = g.inner]<class... X>(X &&...x) -> decltype(auto) {
    if constexpr (sizeof...(X) == 2)
      return [&]<class A, class B>(A &&a, B &&b) {
        return std::pair{f(std::forward<A>(a)), h(std::forward<B>(b))};
      }(std::forward<X>(x)...);
    else
      return [&]<class P>(P &&pp) {
        return std::pair{f(std::get<0>(std::forward<P>(pp))), h(std::get<1>(std::forward<P>(pp)))};
      }(std::forward<X>(x)...);
  }};
}
#endif // TACIT_SIGILS
template <class F, class L> fn(F, L) -> fn<F, L>;

// The comma section proper (declared above) — its vocabulary needs the tables, like `fn`'s.
template <class... Ops> struct comma_section {
  using map = slot_map<Ops...>;
  std::tuple<Ops...> ops;

  static constexpr std::size_t slots = map::slots;

  template <class... F>
    requires(sizeof...(F) == slots)
  [[nodiscard]] constexpr auto operator()(F &&...f) const {
    auto fills = std::forward_as_tuple(std::forward<F>(f)...);
    return [&]<std::size_t... Is>(std::index_sequence<Is...>) {
      // `move(fills)`: same forwarding rule as `section::operator()` — one fill, one slot.
      return std::apply([](auto &&...xs) { return build(std::forward<decltype(xs)>(xs)...); },
                        std::tuple_cat(map::template pick<Is>(ops, std::move(fills))...));
    }(std::make_index_sequence<map::arity>{});
  }

  // Two operands stay a `std::pair` — the documented meaning of the pairing section, and `.first` / `.second` are
  // strictly extra: a pair is a two-tuple for `get`, `tuple_size`, structured bindings, and `apply` alike. Three or
  // more have no pair to be, so they are a `std::tuple`.
  template <class... Xs> [[nodiscard]] static constexpr auto build(Xs &&...xs) {
    if constexpr (sizeof...(Xs) == 2)
      return std::pair{std::forward<Xs>(xs)...};
    else
      return std::tuple{std::forward<Xs>(xs)...};
  }

  // Vocabulary, composing through the built value: `(_, _).swap(p)` is (a, b) -> {a, b}.swap(p). Same shape as `fn`'s
  // member chaining, and arity-preserving in the same way — the result is an `fn` over the same fills, so it keeps
  // chaining (`(_, _).bar().baz()`). Bound arguments only: a blank inside a chained call (`(_, _).foo(_)`) is not a
  // further slot, exactly as it isn't for a projection (`_.front().substr(_)` has never taken one) — the fills belong
  // to the operand list.
#define TACIT_COMMA_MEMBER(NAME)                                                                                       \
  template <class... A>                                                                                                \
    requires(!(tacit::detail::is_blank_v<A> || ...))                                                                   \
  [[nodiscard]] constexpr auto NAME(A &&...a) const {                                                                  \
    return tacit::detail::fn{                                                                                          \
        [c = *this, ... a = std::forward<A>(a)]<class... X>(X &&...x) -> decltype(auto)                                \
          requires requires(X &&...xx, std::decay_t<A> const &...aa) { /* guard == body */                             \
            std::declval<comma_section const &>()(std::forward<X>(xx)...).NAME(aa...);                                 \
          } { return c(std::forward<X>(x)...).NAME(a...); }};                                                          \
  }                                                                                                                    \
  static_assert(true)
#define TACIT_COMMA_FREE1(NAME, FN)                                                                                    \
  [[nodiscard]] constexpr auto NAME() const {                                                                          \
    return tacit::detail::fn{                                                                                          \
        [c = *this]<class... X>(X &&...x) -> decltype(auto)                                                            \
          requires requires(X &&...xx) {                                                                               \
            FN(std::declval<comma_section const &>()(std::forward<X>(xx)...));                                         \
          } { return FN(c(std::forward<X>(x)...)); }};                                                                 \
  }
#define TACIT_COMMA_CPO1(NAME, CPO)                                                                                    \
  [[nodiscard]] constexpr auto NAME() const {                                                                          \
    return tacit::detail::fn{                                                                                          \
        [c = *this]<class... X>(X &&...x) -> decltype(auto)                                                            \
          requires requires(X &&...xx) {                                                                               \
            CPO(std::declval<comma_section const &>()(std::forward<X>(xx)...));                                        \
          } { return CPO(c(std::forward<X>(x)...)); }};                                                                \
  }
  TACIT_STD_MEMBERS(TACIT_COMMA_MEMBER)
  TACIT_FOR_EACH(TACIT_COMMA_MEMBER, TACIT_VERBS)
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_COMMA_MEMBER(NAME);
#define TACIT_FREE(NAME, FN) TACIT_COMMA_FREE1(NAME, FN)
#define TACIT_CPO(NAME, FN) TACIT_COMMA_CPO1(NAME, FN)
#define TACIT_NOUN(NAME)
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
  TACIT_STD_CPOS1(TACIT_COMMA_CPO1)
#undef TACIT_COMMA_MEMBER
#undef TACIT_COMMA_CPO1
#undef TACIT_COMMA_FREE1

  // Comparisons, likewise through the built value: `(_, _) == p` is (a, b) -> {a, b} == p, and `<` is the lexicographic
  // order `pair`/`tuple` already define — the six are the operators those types actually have, which is why the
  // arithmetic and bitwise sets stay off a data builder. The far side must be a value: another comma section, an `fn`,
  // or a blank would be a second operand list rather than something to compare against, so those are left to fail
  // rather than guessed at.
#define TACIT_COMMA_COMPARE(op)                                                                                        \
  template <class Y>                                                                                                   \
    requires(not_comma<Y> && not_fn<Y> && !is_blank_v<Y>)                                                              \
  [[nodiscard]] friend constexpr auto operator op(comma_section c, Y y) {                                              \
    return tacit::detail::fn{[c, y](auto &&...x) -> decltype(auto) {                                                   \
      return c(std::forward<decltype(x)>(x)...) op y;                                                                  \
    }};                                                                                                                \
  }                                                                                                                    \
  template <class X>                                                                                                   \
    requires(not_comma<X> && not_fn<X> && !is_blank_v<X>)                                                              \
  [[nodiscard]] friend constexpr auto operator op(X x, comma_section c) {                                              \
    return tacit::detail::fn{[c, x](auto &&...y) -> decltype(auto) {                                                   \
      return x op c(std::forward<decltype(y)>(y)...);                                                                  \
    }};                                                                                                                \
  }
  TACIT_COMMA_COMPARE(==) TACIT_COMMA_COMPARE(!=) TACIT_COMMA_COMPARE(<)
  TACIT_COMMA_COMPARE(>) TACIT_COMMA_COMPARE(<=) TACIT_COMMA_COMPARE(>=)
#undef TACIT_COMMA_COMPARE

  // Growing the list. `not_comma` on the mixed forms keeps them from competing with the concatenating overload when
  // both sides are comma sections (`(_, _), (_, _)` is a four-slot tuple).
  template <class Y>
    requires not_comma<Y>
  [[nodiscard]] friend constexpr auto operator,(comma_section c, Y y) {
    return std::apply([&](auto const &...o) { return make_comma(o..., std::move(y)); }, c.ops);
  }
  template <class X>
    requires not_comma<X>
  [[nodiscard]] friend constexpr auto operator,(X x, comma_section c) {
    return std::apply([&](auto const &...o) { return make_comma(std::move(x), o...); }, c.ops);
  }
  template <class... B>
  [[nodiscard]] friend constexpr auto operator,(comma_section c, comma_section<B...> d) {
    return std::apply(
        [&](auto const &...a) {
          return std::apply([&](auto const &...b) { return make_comma(a..., b...); }, d.ops);
        },
        c.ops);
  }
};

template <class... A> [[nodiscard]] constexpr comma_section<std::decay_t<A>...> make_comma(A &&...a) {
  return {{std::forward<A>(a)...}};
}
} // namespace detail
// clang-format on

// The arrow proxy behind `_->NAME(...)`. `_`'s operator-> hands back a pointer to this; its vocabulary forwards through
// `->`, so `_->size()` == x -> x->size() — the pointee's member via the *real* operator-> (which is independent of, and
// not always equal to, `(*x).size()`).
namespace detail {
struct arrow {
  TACIT_STD_MEMBERS(TACIT_ARROW_MEMBER)
  // Range access: through `->` these are ordinary member calls on the pointee (no CPO routing — the pointee has the
  // members), so they join the arrow vocabulary as plain forwarders.
  TACIT_ARROW_MEMBER(begin);
  TACIT_ARROW_MEMBER(end);
  TACIT_ARROW_MEMBER(cbegin);
  TACIT_ARROW_MEMBER(cend);
  TACIT_ARROW_MEMBER(rbegin);
  TACIT_ARROW_MEMBER(rend);
  TACIT_ARROW_MEMBER(size);
  TACIT_ARROW_MEMBER(ssize);
  TACIT_ARROW_MEMBER(empty);
  TACIT_ARROW_MEMBER(data);
  TACIT_FOR_EACH(TACIT_ARROW_MEMBER, TACIT_VERBS)
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_ARROW_MEMBER(NAME);
#define TACIT_FREE(NAME, FN)
#define TACIT_CPO(NAME, FN)
#define TACIT_NOUN(NAME)
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
};
inline constexpr arrow arrow_v;
} // namespace detail

// ------------------------------------------------------------------------------------------------ Structural rebind:
// same template, different arguments — `_::rebind<double>::of<std::vector<float>>` is `std::vector<double>`. This is
// the one type-level operation with demand behind it outside metaprogramming libraries: it is what `std::simd`'s
// `rebind_t` does, what allocator `rebind_alloc` does for its own family, and what P3971 is standardising for
// containers generally.
//
// It differs from `blank`/`bind` in the direction it works. Those *build* a specialisation from a template you name;
// rebind *decomposes* one you already have and re-applies its template. You never name `std::vector` — that is the
// point, since the caller usually does not know it.
//
// Arguments are replaced wholesale, which is what you want for defaulted parameters: `std::vector<float>` is really
// `vector<float, allocator<float>>`, and `rebind<double>` yields `vector<double>` — the allocator re-defaults to
// `allocator<double>` rather than being carried across, wrongly, as `allocator<float>`. A second form handles the
// `<class, size_t>` shapes (`array`, `span`), whose extent is not a type and so rides along untouched.
namespace detail {
template <class X, class... New> struct rebind_;
template <template <class...> class F, class... Old, class... New> struct rebind_<F<Old...>, New...> {
  using type = F<New...>;
};
template <template <class, std::size_t> class F, class Old, std::size_t N, class New> struct rebind_<F<Old, N>, New> {
  using type = F<New, N>;
};
} // namespace detail

// ------------------------------------------------------------------------------------------------ The type-level
// blank. One template, two duals: `of` fixes the arguments and awaits the template, `as` fixes the template and awaits
// the arguments — the head-blank and arg-blank grains of the same thing. Both take plain types and plain templates, so
// nothing needs lifting or quoting:
//
//   blank<int>::of<std::vector>           // std::vector<int>   — head is the blank
//   blank<>::as<std::vector>::with<int>   // std::vector<int>   — head is given
//
// `bind<F, Args...>::with<X...>` (below) is the same arg-blank grain with blanks spelled `_::blank<>` among the
// arguments; `as` is the spelling that needs no blank marker at all. (No `$` shorthand exists at the type level —
// `$` is a function, and a qualified name cannot refer into a function-template specialisation.)
template <class... A> struct blank {
  template <template <class...> class F> using of = F<A...>;
  template <template <class...> class F> struct as {
    template <class... X> using with = F<X...>;
  };
};

// The one type `_`: the placeholder's own type. It carries the full std vocabulary (plus any TACIT_VERBS / TACIT_NOUNS)
// and the core for the value side, and doubles as the type-level `_` — a blank for `bind` and a projection namespace
// (`_::name::of<X>`). The value `_` (next) hides the type in ordinary lookup, but a name before `::` is looked up
// considering only types, namespaces, and templates, so `_::name` reaches this struct past the value; elsewhere reach
// the type via `struct _`.
struct _ {
  TACIT_STD_MEMBERS(TACIT_MEMBER)
  TACIT_FOR_EACH(TACIT_MEMBER, TACIT_VERBS)
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_MEMBER(NAME);
#define TACIT_FREE(NAME, FN) TACIT_FREE1(NAME, FN)
#define TACIT_CPO(NAME, FN) TACIT_CPO1(NAME, FN)
#define TACIT_NOUN(NAME) TACIT_TYPE_MEMBER(NAME);
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
  TACIT_STD_CPOS1(TACIT_CPO1)
  TACIT_STD_FREES(TACIT_FREE1)
  TACIT_CPO2(swap, std::ranges::swap)
  /* the type-level blank, reached through the same symbol: `_::blank<int>::of<std::vector>`. `_::`
     is looked up considering only types, namespaces and templates, so it reaches this class past
     the variable `_` — the same route the nouns below take. */
  template <class... A> using blank = tacit::blank<A...>;
  /* structural rebind: `_::rebind<double>::of<std::vector<float>>` == `std::vector<double>`. Named
     for the standard's own vocabulary (simd's `rebind_t`, P3971); a TACIT_NOUN_TEMPLATES entry of
     the same name would collide, since that hook spells the *nested* rebind (`X::template rebind`). */
  template <class... New> struct rebind {
    template <class X> using of = typename tacit::detail::rebind_<X, New...>::type;
  };
  TACIT_STD_TYPE_MEMBERS(TACIT_TYPE_MEMBER)
  TACIT_FOR_EACH(TACIT_TYPE_MEMBER, TACIT_NOUNS)
  TACIT_FOR_EACH(TACIT_TYPE_TEMPLATE, TACIT_NOUN_TEMPLATES)
#ifdef TACIT_VIEWS
  TACIT_VIEW_VERBS(TACIT_VIEW)
#endif
  [[nodiscard]] constexpr const detail::arrow *operator->() const { return &detail::arrow_v; }
  TACIT_CORE(_);

  // FIELD-STYLE verbs: `_.first`, no parens. `pair`'s components are data members, not calls, and a projection of one
  // reads better without empty parentheses — `sort(v, {}, _.first)` rather than `_.first()`. They are static data
  // members holding a closure, which is why they live here rather than in TACIT_CORE: a `static constexpr fn<…>` needs
  // `fn` COMPLETE, and TACIT_CORE is also expanded inside `fn` itself, where it is not. The practical consequence is
  // that field style does not chain FROM a projection (`_.front().first` has no member to find) — spell that hop
  // `_.front().get<0>()`, which is the same access by its other name.
  //
  // Nor does the lift mirror them: `$(p).first` would have to BE the value, and a data member cannot compute one from
  // the subject. `$(p).get<0>()` is the closed-cell spelling. This is the single place `$(x).f() == _.f()(x)` does not
  // apply, and it is because `.first` is not a call on either side — the rule is about calls.
  static constexpr auto first = tacit::detail::fn{[]<class X>(X &&x) -> decltype(auto)
                                                    requires requires(X &&xx) { std::forward<X>(xx).first; }
                                                  { return (std::forward<X>(x).first); }};
  static constexpr auto second = tacit::detail::fn{[]<class X>(X &&x) -> decltype(auto)
                                                     requires requires(X &&xx) { std::forward<X>(xx).second; }
                                                   { return (std::forward<X>(x).second); }};
};
inline constexpr _ _;

// Closure combinators (Haskell arrow flavour). `_`-agnostic — any callable works — and each returns an fn, so results
// keep composing (member access, operator sections, ...).
//   compose(f, g, ...) left-to-right compose  x -> ...(g(f(x)))   (general function composition)
//   fanout(f, g, ...)  x -> tuple{f(x), g(x), ...}         (Haskell &&&)
//   first(f) / second(f)   transform one component of a pair   (Haskell first / second)
//
// These are free functions in `tacit`, reached only by qualification — they never enter anyone's scope uninvited, so
// they cost a caller nothing until spelled. That is why they are UNGATED (a macro gate was tried and dropped): the
// hazards that justify gating elsewhere — `$` is a lexer extension, `λ` is a macro, the sigils spend real operator
// readings — do not exist for plain qualified names, and across a module boundary a gate on the interface build is the
// wrong granularity entirely. The philosophy stands unchanged: the surface you SEE stays `_`.
//
// NOTE: `compose` is where general composition lives now that `operator|` is an ordinary bitwise section (`_ | y`),
// symmetric with `&`, rather than the old `f | g` compose. Member chaining (`_.front().size()`) still composes
// vocabulary on the default surface; `compose` is for composing arbitrary closures — `compose(_ + 1, _ * 2)(3)` == `(3
// + 1) * 2` == 8.
template <class F, class... Gs> [[nodiscard]] constexpr auto compose(F f, Gs... gs) {
  if constexpr (sizeof...(Gs) == 0)
    return detail::fn{f};
  else
    return detail::fn{[f, rest = tacit::compose(gs...)](auto &&x) -> decltype(auto) {
      return rest(f(std::forward<decltype(x)>(x)));
    }};
}
template <class... Fs> [[nodiscard]] constexpr auto fanout(Fs... fs) {
  return detail::fn{[fs...](auto &&x) { return std::tuple{fs(x)...}; }};
}
template <class F> [[nodiscard]] constexpr auto first(F f) {
  return detail::fn{[f](auto &&p) { return std::pair{f(std::get<0>(p)), std::get<1>(p)}; }};
}
template <class F> [[nodiscard]] constexpr auto second(F f) {
  return detail::fn{[f](auto &&p) { return std::pair{std::get<0>(p), f(std::get<1>(p))}; }};
}

// ------------------------------------------------------------------------------------------------
// ------------------------------------------------------------------------------------------------ Type-level tacit:
// `_` doubles as a type-level blank for partially applying a class template into a metafunction. A template argument
// list can't hold the *value* `_`, so the blank is written with the type-level blank `_::blank<>`. Fixed arguments stay
// plain types, no wrapper:
//
//   bind<std::vector, _::blank<>>::with<int>              // std::vector<int>
//   bind<std::map, int, _::blank<>>::with<double>         // std::map<int, double>  (partial)
//   bind<std::map, _::blank<>, _::blank<>>::with<char, int> // std::map<char, int>
//
// The dual of bind is projection: where bind wraps the blank in an *outer* template, `_::name::of<X>` pulls a *member*
// out of X — the type-level twin of the value-level member vocabulary. It is a closed vocabulary (each name is declared
// in `struct _`, table above; add your own via TACIT_NOUNS), applied with `::of` since there is no `operator()` at the
// type level:
//
//   _::value_type::of<std::vector<int>>     // int
//   _::mapped_type::of<std::map<int, char>> // char
//   _::value_type::of<_::value_type::of<T>> // chains by nesting (T's value_type's value_type)
namespace detail {
// The type-level blank is `blank<>` — spelled `_::blank<>` in user code. Neither `struct _` nor `decltype(_)` is a
// blank: the placeholder's own type is the term-level object, and conflating the two is what made the old spelling need
// a `struct` crutch in the first place. Walk the argument list, replacing each `blank<>` blank with the next of Xs...;
// keep fixed types.
template <template <class...> class F, class Done, class Xs, class... Args> struct fill_slots;
template <template <class...> class F, class... D, class Xs> struct fill_slots<F, std::tuple<D...>, Xs> {
  using type = F<D...>;
};
template <template <class...> class F, class... D, class X, class... Xr, class... A>
struct fill_slots<F, std::tuple<D...>, std::tuple<X, Xr...>, tacit::blank<>, A...>
    : fill_slots<F, std::tuple<D..., X>, std::tuple<Xr...>, A...> {};

template <template <class...> class F, class... D, class Xs, class A0, class... A>
struct fill_slots<F, std::tuple<D...>, Xs, A0, A...> : fill_slots<F, std::tuple<D..., A0>, Xs, A...> {};
} // namespace detail

// ------------------------------------------------------------------------------------------------ The term-level lift:
// `lift(x)` gives a plain value the vocabulary it may not have as members, and applies it eagerly. The rule is exactly
//
//     lift(x).f(a...)   ==   _.f(a...)(normalize(x))
//
// so the closed cell is the open one applied now — same table, same dispatch (member, range CPO, or free function), no
// second vocabulary to keep in step. It is what reaches operations a bare value has no members for: `lift(-42).abs()`
// is 42, `lift(v).size()` is `ranges::size(v)` even where `v.size()` does not exist.
//
// NORMALIZE. A string literal's raw type is never what you mean: `const char[4]` has no members at all, and
// `ranges::size("abc")` is 4 because it counts the NUL. So a char array is normalized to `string_view` on the way in,
// making `lift("abc").length()` and `.size()` both 3. Everything else is taken as it is: lvalues are held by reference
// (no copy), rvalues by value (no dangling).
//
// The result of a lifted call is the operation's own natural result, not another lift — `.size()` hands back a
// `size_t`, not a wrapper needing unwrapping. Chaining therefore continues on that result's own type; re-lift with
// `lift(...)` when the next hop needs the vocabulary again.
namespace detail {
template <class T> constexpr decltype(auto) normalize(T &&t) {
  using U = std::remove_cvref_t<T>;
  if constexpr (std::is_array_v<U> && std::is_same_v<std::remove_cv_t<std::remove_extent_t<U>>, char>)
    return std::string_view(t);
  else
    return static_cast<T &&>(t);
}
// The lift's arrow proxy (see `held::operator->`). Members forward through the held value's own `operator->`, eagerly —
// the twin of the `arrow` proxy that `_->` hands back.
template <class T> struct arrow_of {
  T const &p;
  [[nodiscard]] constexpr arrow_of const *operator->() const { return this; }
#define TACIT_HELD_ARROW(NAME)                                                                                         \
  template <class... A>                                                                                                \
    requires(!(tacit::detail::is_blank_v<A> || ...)) &&                                                                \
            requires(T const &q, A &&...aa) { q->NAME(std::forward<A>(aa)...); }                                       \
  [[nodiscard]] constexpr decltype(auto) NAME(A &&...a) const {                                                        \
    return p->NAME(std::forward<A>(a)...);                                                                             \
  }                                                                                                                    \
  static_assert(true)
  TACIT_STD_MEMBERS(TACIT_HELD_ARROW)
  TACIT_HELD_ARROW(begin);
  TACIT_HELD_ARROW(end);
  TACIT_HELD_ARROW(cbegin);
  TACIT_HELD_ARROW(cend);
  TACIT_HELD_ARROW(rbegin);
  TACIT_HELD_ARROW(rend);
  TACIT_HELD_ARROW(size);
  TACIT_HELD_ARROW(ssize);
  TACIT_HELD_ARROW(empty);
  TACIT_HELD_ARROW(data);
  TACIT_FOR_EACH(TACIT_HELD_ARROW, TACIT_VERBS)
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_HELD_ARROW(NAME);
#define TACIT_FREE(NAME, FN)
#define TACIT_CPO(NAME, FN)
#define TACIT_NOUN(NAME)
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
#undef TACIT_HELD_ARROW
};

template <class T> struct held {
  T v;
#define TACIT_HELD_MEMBER(NAME)                                                                                        \
  template <class... A>                                                                                                \
    requires(!(tacit::detail::is_blank_v<A> || ...)) &&                                                                \
            requires(T const &x, A &&...aa) { x.NAME(std::forward<A>(aa)...); }                                        \
  [[nodiscard]] constexpr decltype(auto) NAME(A &&...a) const {                                                        \
    return v.NAME(std::forward<A>(a)...);                                                                              \
  }                                                                                                                    \
  static_assert(true)
#define TACIT_HELD_CPO1(NAME, CPO)                                                                                     \
  [[nodiscard]] constexpr decltype(auto) NAME() const                                                                  \
    requires requires(T const &x) { CPO(x); }                                                                          \
  {                                                                                                                    \
    return CPO(v);                                                                                                     \
  }
#define TACIT_HELD_FREE1(NAME, FN)                                                                                     \
  [[nodiscard]] constexpr decltype(auto) NAME() const                                                                  \
    requires requires(T const &x) { FN(x); }                                                                           \
  {                                                                                                                    \
    return FN(v);                                                                                                      \
  }
  TACIT_STD_MEMBERS(TACIT_HELD_MEMBER)
  TACIT_FOR_EACH(TACIT_HELD_MEMBER, TACIT_VERBS)
#ifdef TACIT_VOCABULARY_FILE
#define TACIT_VERB(NAME) TACIT_HELD_MEMBER(NAME);
#define TACIT_FREE(NAME, FN) TACIT_HELD_FREE1(NAME, FN)
#define TACIT_CPO(NAME, FN) TACIT_HELD_CPO1(NAME, FN)
#define TACIT_NOUN(NAME)
#include TACIT_VOCABULARY_FILE
#undef TACIT_VERB
#undef TACIT_FREE
#undef TACIT_CPO
#undef TACIT_NOUN
#endif
  TACIT_STD_CPOS1(TACIT_HELD_CPO1)
  TACIT_STD_FREES(TACIT_HELD_FREE1)
#undef TACIT_HELD_MEMBER
#undef TACIT_HELD_CPO1
#undef TACIT_HELD_FREE1
#define TACIT_HELD_TFREE1(NAME)                                                                                        \
  template <class U>                                                                                                   \
    requires has_                                                                                                      \
  ##NAME<U, T const &> [[nodiscard]] constexpr decltype(auto) NAME() const {                                           \
    return tacit::detail::NAME##_<U>(v);                                                                               \
  }
  TACIT_STD_TFREES(TACIT_HELD_TFREE1)
#undef TACIT_HELD_TFREE1
#if TACIT_HAS_RANGES_TO
  template <template <class...> class C>
    requires requires(T const &x) { std::ranges::to<C>(x); }
  [[nodiscard]] constexpr decltype(auto) to() const {
    return std::ranges::to<C>(v);
  }
  template <class C>
    requires requires(T const &x) { std::ranges::to<C>(x); }
  [[nodiscard]] constexpr decltype(auto) to() const {
    return std::ranges::to<C>(v);
  }
#endif

  // `$(t).get<0>()` — the lift's half of the tuple-like projection, so the rule `$(x).f(a…)` == `_.f(a…)(x)` holds for
  // the one verb whose argument is a template argument. Non-static and `const`, matching the member table, so these
  // overload with the plain `get` rather than clashing with it (a static and a non-static member function of the same
  // name and parameter list cannot coexist).
  template <std::size_t I>
    requires has_get_at<I, T const &>
  [[nodiscard]] constexpr decltype(auto) get() const {
    return tacit::detail::get_<I>(v);
  }
  template <class U>
    requires has_get_type<U, T const &>
  [[nodiscard]] constexpr decltype(auto) get() const {
    return tacit::detail::get_<U>(v);
  }

  // `$(p)->size()` — the arrow surface of the lift, for the case a value has no useful members of its own: a smart
  // pointer, an iterator, an optional-ish handle. It mirrors `_->size()` exactly, reaching the pointee through the
  // *real* `operator->` and calling plain members there (no CPO routing — the pointee has the members), so `$(p)->f()`
  // is `p->f()` and nothing more.
  //
  // `operator->` hands back a proxy by value whose own `operator->` returns itself; the compiler repeats `->` until it
  // reaches a raw pointer, and the temporary lives to the end of the full expression. This is the usual proxy-arrow
  // idiom, and it is why the lift needs no storage for it.
  [[nodiscard]] constexpr auto operator->() const { return arrow_of<T>{v}; }

  // The held subject, unchanged. NOT named `get`: `get` is in the vocabulary (shared_ptr, unique_ptr, future), and an
  // accessor of that name would shadow it, so `$(p).get()` would hand back the pointer holder rather than `p.get()`.
  // Any accessor added here must stay out of the vocabulary's namespace for the same reason — the rule `$(x).f() ==
  // _.f()(x)` has to hold for *every* name.
  [[nodiscard]] constexpr T const &subject() const noexcept { return v; }
};
template <class T> held(T) -> held<T>;
} // namespace detail

template <class X> [[nodiscard]] constexpr auto lift(X &&x) {
  using N = decltype(detail::normalize(static_cast<X &&>(x)));
  if constexpr (std::is_lvalue_reference_v<X> && std::is_lvalue_reference_v<N>)
    return detail::held<std::remove_reference_t<N> &>{detail::normalize(static_cast<X &&>(x))};
  else
    return detail::held<std::remove_cvref_t<N>>{detail::normalize(static_cast<X &&>(x))};
}

template <template <class...> class F, class... Args> struct bind {
  template <class... Xs> using with = typename detail::fill_slots<F, std::tuple<>, std::tuple<Xs...>, Args...>::type;
};

// The general primitive that subsumes `bind` and curries BOTH grains under one op. `bind` fixes the template and blanks
// among its *arguments*; `apply` additionally lets the *template itself* be a blank. Quote a template into a type with
// `quote<F>` (a template can't sit in a type slot unquoted — packs are single-kind), then
// `apply<Slots...>::with<Fills...>` fills each `blank<>` slot — template or argument — left to right:
//
//   apply<quote<std::map>, _::blank<>, _::blank<>>::with<int, char> // std::map<int,char> (fix template)
//   apply<_::blank<>, int, char>::with<quote<std::map>>            // std::map<int,char> (fix args)
//   apply<_::blank<>, int, _::blank<>>::with<quote<std::map>, char> // std::map<int,char> (blank both)
//
// So `bind<F, A...>::with<X...>` is `apply<quote<F>, A...>::with<X...>`; the arg-first grain is the mirror the plain
// `bind` can't spell. A C++26 reflection build would erase `quote<>` — templates and types both become std::meta::info,
// so the slot list stops needing the wrapper. (Naming provisional.)
template <template <class...> class F> struct quote {};
namespace detail {
template <class... Applied> struct run_slots; // first Applied slot must resolve to a quote<F>
template <template <class...> class F, class... A> struct run_slots<quote<F>, A...> {
  using type = F<A...>;
};
template <class Acc, class Fills, class... Slots> struct apply_slots;
template <class... Acc, class Fills> struct apply_slots<std::tuple<Acc...>, Fills> {
  using type = typename run_slots<Acc...>::type;
};
template <class... Acc, class X, class... Xr, class... S>
struct apply_slots<std::tuple<Acc...>, std::tuple<X, Xr...>, tacit::blank<>, S...>
    : apply_slots<std::tuple<Acc..., X>, std::tuple<Xr...>, S...> {};
template <class... Acc, class Fills, class S0, class... S>
struct apply_slots<std::tuple<Acc...>, Fills, S0, S...> : apply_slots<std::tuple<Acc..., S0>, Fills, S...> {};
} // namespace detail

template <class... Slots> struct apply {
  template <class... Fills>
  using with = typename detail::apply_slots<std::tuple<>, std::tuple<Fills...>, Slots...>::type;
};

#if TACIT_HAS_REFLECTION
// C++26 extension point: with a P2996 toolchain, std::meta::substitute generalizes this to alias templates and non-type
// template parameters that a template-template parameter cannot name. Left a documented hook rather than shipped blind
// — untested reflection metaprograms want a real toolchain, same posture as the reflective member hatch.
#endif

// ------------------------------------------------------------------------------------------------ `make` — the closed
// cell that BUILDS a value, where `lift` adopts one that already exists:
//
//   make<std::vector>(1, 2, 3)                    // std::vector<int>{1,2,3}     — plain CTAD
//   make<std::vector, double>(1.0, 2.0)           // std::vector<double>         — arguments given
//   make<std::set, _, std::greater<>>(3, 1, 2)    // std::set<int, greater<>>    — PARTIAL CTAD
//
// The third line is the one C++ cannot otherwise spell. CTAD is all-or-nothing: the moment you want to fix *one*
// template argument you must write them all, including the ones the compiler was about to work out for you
// (`std::set<int, std::greater<>>{3,1,2}` — the `int` is pure re-typing, and it is the argument most likely to change).
// A `_` in the list means "deduce this position"; everything else is fixed, and trailing parameters you do not mention
// re-default as usual.
//
// It works by deducing the whole specialisation and then overlaying the positions the caller fixed — the same
// structural decomposition `rebind` does, so the two share a notion of "same template, different arguments". Deduction
// runs first and unmodified, which is what keeps the deduced positions exactly what plain CTAD would have given.
//
// `make` returns the value ITSELF, not a lift of it. `lift(x)` is a view of a subject that outlives it, so it must not
// copy; `make<F>(a...)` creates the subject, so it must own. A lift of a temporary would dangle at the semicolon, and a
// caller writing `auto v = make<std::vector>(1,2,3)` wants a `std::vector` they can hand to anything — not a wrapper.
// Call `.f()` on it and you are calling the real member; wrap it in `lift`/`$` if you want the vocabulary.
//
// HOW `_` SITS AMONG TYPES. A template parameter list is single-kind — one pack holds types or values, never both — so
// `<std::map, _, int>` cannot be one variadic parameter: `_` is a *value* (name lookup finds the variable, so the
// argument parses as an expression) and `int` is a type. Function templates are the one construct that escapes this,
// because they can be OVERLOADED on parameter kind, which class templates cannot be. So the blank patterns are
// enumerated: one overload per arrangement of blanks, each ending in a trailing `class... B` pack that soaks up the
// fixed arguments after the last blank. All-types lists need no enumeration at all — the plain variadic overload takes
// them — so only lists that actually contain a blank pay for it.
//
// Depth 4 is enough for the standard library and then some: a blank means "deduce", and a *trailing* deduced argument
// is already deduced by simply not writing it, so blanks only ever need to reach as far as the last argument you fix.
// Four covers the deepest real case — `unordered_map`'s <Key, T, Hash, KeyEqual> ahead of a fixed allocator. Past that,
// `bind`/`apply` still spell it at type level, and a blank nobody enumerated is a hard error rather than a wrong
// answer.
//
// SHAPE. `F` is `template <class...> class`, so the `<class, std::size_t>` families (`array`, `span`) are out of reach
// — as they are for any template-template parameter. Nothing is lost: their extent is deduced and their one type
// argument is the only thing partial CTAD could fix, so plain CTAD (`std::array{1,2,3}`) already says everything.
// `rebind` covers that shape at type level, where the second, non-type parameter can be matched explicitly.
namespace detail {
template <class T>
concept blank_value = is_blank_v<T>;

// Split a deduced specialisation into its template and its arguments (cf. `rebind_`, which replaces them wholesale;
// here each position is replaced only if the caller fixed it).
template <class X> struct parts_of;
template <template <class...> class F, class... A> struct parts_of<F<A...>> {
  template <class... X> using rebound = F<X...>;
  using args = std::tuple<A...>;
};
template <class Fixed, class Deduced> struct pick_slot {
  using type = Fixed;
};
template <class Deduced> struct pick_slot<tacit::blank<>, Deduced> {
  using type = Deduced;
};
// Position I: the caller's type if they named one there, the deduced type if they wrote `_` or ran off the end of the
// fixed list.
template <std::size_t I, class Fixed, class Deduced> struct slot_at {
  static constexpr bool named = I < std::tuple_size_v<Fixed>;
  using type = typename pick_slot<
      std::tuple_element_t<named ? I : 0, std::conditional_t<named, Fixed, std::tuple<tacit::blank<>>>>,
      std::tuple_element_t<I, Deduced>>::type;
};
template <class Deduced, class Fixed, class Idx> struct overlay_;
template <class Deduced, class Fixed, std::size_t... I> struct overlay_<Deduced, Fixed, std::index_sequence<I...>> {
  using type = typename parts_of<Deduced>::template rebound<
      typename slot_at<I, Fixed, typename parts_of<Deduced>::args>::type...>;
};
template <class Deduced, class... Fixed>
using overlay = typename overlay_<Deduced, std::tuple<Fixed...>,
                                  std::make_index_sequence<std::tuple_size_v<typename parts_of<Deduced>::args>>>::type;

template <template <class...> class F, class... Fixed, class... A> [[nodiscard]] constexpr auto make_(A &&...a) {
  if constexpr (sizeof...(Fixed) == 0)
    return F{static_cast<A &&>(a)...}; // nothing fixed: plain CTAD, deduction guides and all
  // NB `is_same_v<_, blank>`, not `is_blank_v` — by the time a `_` argument reaches here it has been rewritten to the
  // type-level marker `blank`; `is_blank_v` answers for the placeholder VALUE's type.
  else if constexpr (!(std::is_same_v<Fixed, tacit::blank<>> || ...))
    return F<Fixed...>{static_cast<A &&>(a)...}; // nothing deduced: the arguments as given
  else                                           // both: deduce, then overlay what was fixed
    return overlay<decltype(F{std::declval<A>()...}), Fixed...>{static_cast<A &&>(a)...};
}
} // namespace detail

// The overload-set generator lives in its own (guard-free, macro-only) header so <tacit/$.hpp> can
// replay it verbatim as `$` — same shapes, no repetition, no include-order sensitivity. It is
// consumed and #undef'd here like every other generator.
// The `make` overload-set generator, shared verbatim by <tacit/_.hpp> (as `tacit::make`) and
// <tacit/$.hpp> (as `tacit::$`). Macros only — no declarations — so it has NO include guard: each
// consumer includes it, expands TACIT_MAKE_OVERLOADS(name), and #undefs the five macros again,
// keeping the clean-path promise that no generator macro survives an include. That is also why the
// two public headers can be included in either order: neither depends on the other having left
// macros behind. (The inner #ifndef only suppresses redefinition if a consumer forgets the #undef.)
//
// TACIT_MAKE_OVERLOADS(NAME) emits `NAME<F, ...>(a...)` == partial CTAD: every `tacit::blank<>` (a
// `_` among the explicit template arguments arrives as one) means "deduce this position", to depth
// four. The engine is `tacit::detail::make_` in <tacit/_.hpp>; these macros only spell the sixteen
// type/value parameter shapes the language cannot express as one variadic signature (a single pack
// cannot mix type and non-type parameters — the same kind wall the type-level notes describe).
#ifndef TACIT_MAKE_OVERLOADS
#define TACIT_UNPAREN(...) __VA_ARGS__
#define TACIT_MAKE_BLANK tacit::blank<>                 /* the fixed-list marker: deduce here */
#define TACIT_MAKE_SLOT tacit::detail::blank_value auto /* the parameter slot a `_` argument binds */
#define TACIT_MAKE_ONE(NAME, PARAMS, FIXED)                                                                            \
  template <template <class...> class F, TACIT_UNPAREN PARAMS, class... B>                                             \
  [[nodiscard]] constexpr auto NAME(auto &&...a) {                                                                     \
    return tacit::detail::make_<F, TACIT_UNPAREN FIXED, B...>(static_cast<decltype(a)>(a)...);                         \
  }
#define TACIT_MAKE_OVERLOADS(NAME)                                                                                     \
  template <template <class...> class F, class... B> [[nodiscard]] constexpr auto NAME(auto &&...a) {                  \
    return tacit::detail::make_<F, B...>(static_cast<decltype(a)>(a)...);                                              \
  }                                                                                                                    \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0), (TACIT_MAKE_BLANK))                                                       \
  TACIT_MAKE_ONE(NAME, (class A0, TACIT_MAKE_SLOT A1), (A0, TACIT_MAKE_BLANK))                                         \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, TACIT_MAKE_SLOT A1), (TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))                 \
  TACIT_MAKE_ONE(NAME, (class A0, class A1, TACIT_MAKE_SLOT A2), (A0, A1, TACIT_MAKE_BLANK))                           \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, class A1, TACIT_MAKE_SLOT A2), (TACIT_MAKE_BLANK, A1, TACIT_MAKE_BLANK))   \
  TACIT_MAKE_ONE(NAME, (class A0, TACIT_MAKE_SLOT A1, TACIT_MAKE_SLOT A2), (A0, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))   \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, TACIT_MAKE_SLOT A1, TACIT_MAKE_SLOT A2),                                   \
                 (TACIT_MAKE_BLANK, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))                                               \
  TACIT_MAKE_ONE(NAME, (class A0, class A1, class A2, TACIT_MAKE_SLOT A3), (A0, A1, A2, TACIT_MAKE_BLANK))             \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, class A1, class A2, TACIT_MAKE_SLOT A3),                                   \
                 (TACIT_MAKE_BLANK, A1, A2, TACIT_MAKE_BLANK))                                                         \
  TACIT_MAKE_ONE(NAME, (class A0, TACIT_MAKE_SLOT A1, class A2, TACIT_MAKE_SLOT A3),                                   \
                 (A0, TACIT_MAKE_BLANK, A2, TACIT_MAKE_BLANK))                                                         \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, TACIT_MAKE_SLOT A1, class A2, TACIT_MAKE_SLOT A3),                         \
                 (TACIT_MAKE_BLANK, TACIT_MAKE_BLANK, A2, TACIT_MAKE_BLANK))                                           \
  TACIT_MAKE_ONE(NAME, (class A0, class A1, TACIT_MAKE_SLOT A2, TACIT_MAKE_SLOT A3),                                   \
                 (A0, A1, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))                                                         \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, class A1, TACIT_MAKE_SLOT A2, TACIT_MAKE_SLOT A3),                         \
                 (TACIT_MAKE_BLANK, A1, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))                                           \
  TACIT_MAKE_ONE(NAME, (class A0, TACIT_MAKE_SLOT A1, TACIT_MAKE_SLOT A2, TACIT_MAKE_SLOT A3),                         \
                 (A0, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))                                           \
  TACIT_MAKE_ONE(NAME, (TACIT_MAKE_SLOT A0, TACIT_MAKE_SLOT A1, TACIT_MAKE_SLOT A2, TACIT_MAKE_SLOT A3),               \
                 (TACIT_MAKE_BLANK, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK, TACIT_MAKE_BLANK))
#endif // TACIT_MAKE_OVERLOADS

TACIT_MAKE_OVERLOADS(make)

// Heterogeneous element combinators: drive a callable over the elements of a tuple-like. Built on std::apply +
// fold-expressions (C++23); a `template for` (C++26) path can later extend them to arbitrary aggregates and reflection
// ranges. They are `_`-agnostic (any callable works) but pair naturally with `_`'s closures, e.g. transform_elements(t,
// _.size()) or any_of_element(t, _.empty()). Free `tacit::` functions, reached only by qualification —
// ungated, like the closure combinators above and for the same reason.
template <class Tup, class F> constexpr void for_each_element(Tup &&t, F &&f) {
  // `void(...)` so the fold uses the built-in comma even when f returns something with an `operator,` of its own — an
  // `fn` or a comma section would otherwise accumulate here.
  std::apply([&](auto &&...xs) { (void(f(std::forward<decltype(xs)>(xs))), ...); }, std::forward<Tup>(t));
}
template <class Tup, class F> [[nodiscard]] constexpr bool any_of_element(Tup &&t, F &&f) {
  return std::apply([&](auto &&...xs) { return (static_cast<bool>(f(std::forward<decltype(xs)>(xs))) || ...); },
                    std::forward<Tup>(t));
}
template <class Tup, class F> [[nodiscard]] constexpr bool all_of_element(Tup &&t, F &&f) {
  return std::apply([&](auto &&...xs) { return (static_cast<bool>(f(std::forward<decltype(xs)>(xs))) && ...); },
                    std::forward<Tup>(t));
}
template <class Tup, class F> [[nodiscard]] constexpr bool none_of_element(Tup &&t, F &&f) {
  return !tacit::any_of_element(std::forward<Tup>(t), std::forward<F>(f));
}
template <class Tup, class F> [[nodiscard]] constexpr auto transform_elements(Tup &&t, F &&f) {
  return std::apply([&](auto &&...xs) { return std::tuple{f(std::forward<decltype(xs)>(xs))...}; },
                    std::forward<Tup>(t));
}

} // namespace tacit

// Two surfaces deliberately live in sibling headers rather than here:
//   - `$` — the canonical short name for the term wrapper (`$(x)` == `lift(x)`, `$<F>(a...)` ==
//     `make<F>(a...)`) — is in <tacit/$.hpp>. `$` in an identifier is a GCC/Clang extension,
//     rejected under `-pedantic-errors`, so including that header IS the opt-in and this one stays
//     strictly conforming. Nothing is `$`-only: `lift`/`make` here are the same functions.
//   - The natural-spelling std blanks (`std::map<struct _, int>::with<char>`, experimental) are in
//     <tacit/experimental/std_blanks.hpp> — they specialize std templates, which is [namespace.std]
//     deviancy this header will not carry.

// The default path exports exactly one name, `tacit::_`; a single `using tacit::_;` is all a
// caller needs — the vocabulary is reached through the object and the operator sections are hidden friends found by
// ADL. `_` is the only placeholder: domain names are taught to it in place with the pre-#define TACIT_VERBS /
// TACIT_NOUNS hooks (consumed above, at include time), so nothing downstream needs the internal generator macros — the
// header always cleans them up here, one path, no TACIT_KEEP_MACROS switch.
//
// Two macros survive on the clean path, both feature flags rather than generators, so you can `#if` on what the
// toolchain actually gave you without re-deriving tacit's `__cpp_*` conditions yourself: `TACIT_HAS_REFLECTION` (are
// the reflective members m / field / enum_name / each_field present?) and `TACIT_HAS_RANGES_TO` (is `_.to<C>()`
// present? — `std::ranges::to` is C++23 but shipped in libstdc++ 14 and libc++ 17, so g++-13 builds do not have it).
#undef TACIT_TO_MEMBERS
#undef TACIT_ADL_TFN
#undef TACIT_TFREE1
#undef TACIT_STD_TFREES
#undef TACIT_MARK_INNER
#undef TACIT_PLAIN
#undef TACIT_MARK_AMP
#undef TACIT_MARK_STAR
#undef TACIT_UNARY_AS
#undef TACIT_FN_UNARY_AS
#undef TACIT_UNPAREN
#undef TACIT_MAKE_BLANK
#undef TACIT_MAKE_SLOT
#undef TACIT_MAKE_ONE
#undef TACIT_MAKE_OVERLOADS
#undef TACIT_MEMBER
#undef TACIT_ARROW_MEMBER
#undef TACIT_VIEW
#undef TACIT_VIEW_VERBS
#undef TACIT_CPO1
#undef TACIT_CPO2
#undef TACIT_FREE1
#undef TACIT_STD_FREES
#undef TACIT_SECTION
#undef TACIT_COMPARE
#undef TACIT_MEMPTR
#undef TACIT_UNARY
#undef TACIT_UNARY_POST
#undef TACIT_ASSIGN
#undef TACIT_REFLECT
#undef TACIT_CORE
#undef TACIT_STD_MEMBERS
#undef TACIT_STD_CPOS1
#undef TACIT_TYPE_MEMBER
#undef TACIT_TYPE_TEMPLATE
#undef TACIT_STD_TYPE_MEMBERS
#undef TACIT_FE
#undef TACIT_FE_AGAIN
#undef TACIT_FOR_EACH
#undef TACIT_PARENS
#undef TACIT_EXPAND
#undef TACIT_EXPAND_A
#undef TACIT_EXPAND_B
#undef TACIT_EXPAND_C
#undef TACIT_VOCABULARY_FILE
#undef TACIT_VERBS
#undef TACIT_NOUNS
#undef TACIT_NOUN_TEMPLATES
#endif // TACIT_SINGLE_CORE_SEEN
